{
  "sprint": "calib-route-a1",
  "features": [
    {
      "id": "F001",
      "title": "真值夹具（≥10 对应 + 留一法交叉验证 → 参考相机与不确定度）",
      "priority": "high",
      "executor": "generator",
      "status": "in_progress",
      "blocked_on": "用户人工指认对应点（工具链已就绪, 见 60fcab8）",
      "depends": [],
      "acceptance": "**方向 spec §D1 + §D1.1 订正(2026-07-21 用户裁决: 改用仅地面平面)。无真值不开工 —— 这是 b3 五轮不收敛的根因。** 建 scripts/calib_truth/: 输入一张照片 + 人工标注的 **≥5 个地面点**(墙脚线转角 + 门洞下沿), 单平面单应分解出 f/R/t, 留一法交叉验证, **留出点的重投影误差即该真值的不确定度**。**世界坐标必须零假设** —— 只用 z=0(x/y 来自平面图), 不得混入天花 2700 / 门头 2050 这类假设值(§D1.1 实证: 混合高度方案在真实标注上残差 60px, 且层高本身三个方法三个答案)。**自检门必须实装**: 两路 f 估计各用留一 CV 判稳定性(<10%), 至少一路稳定, 两路都稳定时须互相印证(≤15%) —— 平面正对相机时会解出 +3754% 的荒谬 f 却不报错。**产出 2 张照片的夹具, 覆盖两枚镜头**(spec §6.1 已证 f 非常数): 超广角与主摄各一。JSON 仅数值。**PIPL: 只存相机参数 + 对应坐标 + 照片 sha256, 像素绝不入 git。** 验收: 不确定度须报出具体数字; 过大须明说该照片不适合当真值并换一张 —— **不得为凑数把不可信的当真值**。pytest 同 commit。",
      "notes": "需用户配合标注地面点。**候选照片(仅地面点数)**: r_live 19 / r_cloak 9 / r_study 7 / r_master 6 / r_garden 6, 均 ≥5。工具链见 60fcab8 + 本次订正: solve_plane.py(单平面) / world_points.floor_candidates / build_fixture.py --mode plane(默认) / mark.html 地面模式。**注意 f 有 EXIF 可作外部真值而 R 没有 —— 本条的主要价值在于给 R 建真值**(spec §5.4)。"
    },
    {
      "id": "F002",
      "title": "线段提取 + 三组正交消失点聚类 → 估计 R 与 f",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "depends": [
        "F001"
      ],
      "acceptance": "**方向 spec §D2/D3。研究码 scripts/spike/, 不 import main.py, 零产品代码改动。** 从照片提线段 → RANSAC 聚成三组正交消失点(Manhattan) → 解出旋转 R 与焦距 f。**§6 调查已完成, 结论见 spec §6.1 —— 本条是把 scratchpad 研究码正式化并补齐 R**: (a) cv2 LSD 5.0.0 可用且已验证; (b) 必须实装自检门(三对正交 f 齐备且 spread<15%), 裸方法 12/12 从不拒解是危险的; (c) 纯 numpy 路径已证**未可行**(过拟合 + 自检假阴性 + 501MB 峰值内存), 若仍要评估须按正经移植 LSD 而非朴素 Hough; (d) **R 的估计与误差量化是本条重点** —— f 已有 EXIF 兜底, R 没有(spec §5.4)。**本批不解位置(剩余 3 DoF)**。",
      "notes": "室内墙/地/天花天然三组正交。这一步把 7 个自由度里的 4 个从人手里拿走, 不依赖任何人工对应。**自检门属事后拟合(spec §5.3), F002 不得自证其泛化性 —— 那是 F003 在留出照片上的事。**"
    },
    {
      "id": "F003",
      "title": "在真值夹具上量化误差 + 失效表现 → go/no-go 报告",
      "priority": "high",
      "executor": "evaluator",
      "status": "pending",
      "depends": [
        "F001",
        "F002"
      ],
      "acceptance": "隔离 evaluator 独立验收(无自评)。产出 docs/test-reports/calib-route-a1-eval-YYYYMMDD.md: (1) 每张真值照片上的 **R 误差(角度) / f 误差(%) / 成功率**; (2) **Manhattan 假设失效时是拒解还是错解**, 给数字 —— 这条比精度更重要; (3) 若产品化不引 cv2, 纯 numpy 精度损失多少; (4) 诚实边界(样本量/不适用的照片/真值自身不确定度); (5) **go/no-go + 下一批建议**(含位置 3 DoF 怎么办); (6) 红线核查: 未改 apps/api 与 apps/web 产品路径, 未写 data/projects/, PIPL 未入 git。**另需复验 b3 遗留**: F007-F010 当时由用户 L2 驱动当场修复, **未经隔离验收**, 本批一并补验。",
      "notes": "R 误差多大算『可用』须反推 —— 可复用 b3 F005 的引导图作判据(相机误差 → 引导崩坏程度)。"
    }
  ]
}