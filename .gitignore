# 系统 / Python
.DS_Store
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# 大体积 / 可由引擎重生成的渲染产物(磁盘保留,不入库)
*.mp4
平面布置图.png
平面布置图-无家具.png
效果图-*.png
bfe6904f*.png
轴测图POC/*.png
轴测图POC/legacy/*.png
轴测图POC/legacy/*.svg
轴测图POC/tests/baseline/*.png

# 未来:内容寻址产物目录
artifacts/

# 用户上传图 (第6步空房实拍照): PIPL 敏感, 永不入库; 生产走 bind 卷 + 可删除
data/uploads/

# health 探针临时文件 (/api/health 写删 DATA_DIR/.health-<pid>.tmp; 崩溃残留兜底)
.health-*.tmp
data/projects/.health-*.tmp

# 项目锁文件 (fcntl.flock 持久锁句柄; 运行时在 DATA_DIR/{project}/.project.lock, 内核管锁)
.project.lock

# 产品化 (apps/web Next.js + apps/api)
node_modules/
.next/
apps/web/.next/
.env
*.tsbuildinfo

# Python venv
apps/api/.venv/
.venv/
.phase0-baseline/
.e2e-sandbox/
apps/web/test-results/
apps/api/test-results/
apps/web/playwright-report/
.agent-id
