已核实三项关键事实(消除误报):studio 代码**零引用** Horizon 组件(grep 空);`FloorplanPreview.tsx` 是**死代码**(全仓零引用);`InputField.tsx:43` 确有 `value={value && value}` 的 0 值 bug。下面是合并三视角、去重去误报后的最终清单。

---

# 阅天府 studio 前端重构清单(最终版)

## 直接回答两问

### Q1 —「可用模板组件却手写」(逐条:文件 → 应换 Horizon 组件)

| # | 文件/位置 | 现状 | 应换组件 | 备注 |
|---|---|---|---|---|
| 1 | `editor/geometry/GeometrySidePanel.tsx:55`、`editor/furniture/FurnitureSidePanel.tsx:46`、`editor/FloorplanEditor.tsx:862` | 手写卡片壳 `rounded-2xl border bg-white dark:bg-navy-800` | `components/card` 的 `<Card extra="...">` | 三处(FloorplanPreview 的第 4 处随删文件消失)。Card 默认 `shadow-3xl`+无 border,要保持"描边扁平"观感用 `extra` 覆盖——属可接受设计取舍 |
| 2 | `GeometrySidePanel.tsx:210-218` | `cut`(布尔)做成 `<select><option value="true/false">` | `components/switch` 的 `Switch`/`SwitchField` | 现还有字符串 `'true'`→boolean 转换,既丑又易错 |
| 3 | `GeometrySidePanel.tsx:342` | 禁用原因用原生 `title=` | `components/tooltip` 的 `TooltipHorizon` | 低优先,仅一处 |
| 4 | `FloorplanEditor.tsx:113-156, 953-957` | 手写 toast state + setTimeout + 固定定位 div | Chakra `useToast()`(provider 已挂载) | 低优先,现实现可用 |

**明确不算 Q1(Horizon 无对应件,手写合理,勿改)**:所有 SVG 图元(RoomRect/ResizeHandles/OpeningMarker/各 Layer/EditorStage/FurnitureStage)、原生 `<select>` 取值控件(Horizon 的 `dropdown` 是菜单弹层非受控 select)、裸 `<button class="bg-brand-500">`(Horizon 自身 sign-in 页也这么写,且无 Button 组件)、StatusBadge/Tab(Horizon 无 Badge/Tabs)。

**特别提示——不要换 InputField**:表面上密集属性面板的 `<input>` 该换 `components/fields/InputField`,但(a) InputField 是 `h-12 rounded-xl` 的 auth 表单尺寸,不适合 340px 密集面板;(b) `InputField.tsx:43` `value={value && value}` 有 **0 值 bug**——room.rect 的 x/y/at/span 可为 0,会被渲染成空丢值。正确做法见 Q2-#1(自抽紧凑字段组件)。

### Q2 —「应抽公共组件却各自重复」(逐条:重复点 → 建议组件)

| # | 重复点(文件:行) | 建议公共组件 |
|---|---|---|
| 1 | `labelCls`/`inputCls` 常量在两面板逐字复制(`GeometrySidePanel.tsx:38-40` ≡ `FurnitureSidePanel.tsx:23-25`),基于它的 label+控件行重复 ~28 次,`{ARR.map(<option>)}` 下拉模式重复 ~10 次 | `studio/fields/`:`Field`/`TextRow`/`NumberRow`/`SelectRow<T>`(紧凑版,**非** Horizon InputField) |
| 2 | 侧栏外壳 className 逐字一致(`GeometrySidePanel.tsx:55` ≡ `FurnitureSidePanel.tsx:46`)+ `<h2>` 一致;内层分区卡 `rounded-xl border-gray-100 p-3` 重复 4 次(`Geo:82,327`、`Furn:80,184`) | `SidePanel`(可基于 `Card`,顺带答 Q1-#1)、`PanelSection` |
| 3 | SVG 画布外壳 3 处重复(`EditorStage.tsx:74-92`、`FurnitureStage.tsx:41-59`、`FloorplanPreview.tsx:83-97`)+ 硬编码底色 `#0b1437` ×3 + `data-bg` 背景捕获 rect | `StageSvg`/`FloorplanCanvas`(viewBox + pointer 回调 + children),底色提 `CANVAS_BG` |
| 4 | **`FloorplanPreview.tsx` 整体是 EditorStage 只读复制**:自带 `WallLine`(103-121)与 `DerivedWallsLayer` 的 WallLine 重复;重声明 `FALLBACK_VIEWBOX/ORIGIN`(已在 `coords.ts:6-7`);手写 viewbox 读取不走 `readViewBox/readOrigin`;类型从 `studioApi` 而非 `lib/floorplan/types` 分叉 | **直接删除**(死代码,已核实零引用)→ 一次消除多条重复 |
| 5 | 房间色块+标签渲染重复:`FurnitureStage.tsx:62-91` 手写一份(与 `RoomRect.tsx:30-69` 同构,仅 opacity/色不同) | 给 `RoomRect` 加 `readOnly?`/`dim?` prop,抽 `RoomsLayer`;FurnitureStage 改用 `<RoomsLayer readOnly>` |
| 6 | 激活态药丸按钮 3 份(`GeometrySidePanel.tsx:357-379` ToolBtn、`FurnitureSidePanel.tsx:135-148` orient、`FloorplanEditor.tsx:831-849` Tab) | `ToggleButton` + `SegmentedControl<T>`(给 Tab/orient 这种互斥组) |
| 7 | 保存按钮三态(`Geo:338-352`≡`Furn:194-201`)、危险删除按钮(`Geo:381-397` DangerBtn ≡ `Furn:170-176` 内联) | `SaveButton`/`DangerButton`(把已有 DangerBtn 提公共目录) |
| 8 | 校验状态行 红/绿/灰三态(`Geo:399-446` StatusBlock ≡ `Furn:184-192`) | `StatusLines({errors,warns,okText,hintText})` |
| 9 | 后端加载失败红 banner(`FloorplanEditor.tsx:852-859` ≡ `FloorplanPreview.tsx:71-80`,两处 `dark:bg-red-950` 写法已 drift) | `BackendErrorBanner`/`LoadErrorPanel` |
| 10 | loadState 徽章两套(`FloorplanEditor.tsx:808-822` 内联 ≡ `FloorplanPreview.tsx:123-131` StatusBadge) | `LoadStateBadge`(沿用 Preview 的 Record map) |
| 11 | `isBg` 判定重复(`FloorplanEditor.tsx:326-327`≡`446-449`);清空选择对象手写多次,虽有 `EMPTY_SELECTION:79` 却没复用(254-313,344-349) | 纯函数 `isBackgroundTarget(e)` + 复用 `EMPTY_SELECTION` |
| 12 | SVG 选中橙 `#e0701a` 散落 6 文件、红 `#dc2626`、墙色 `#444/#9a9a9a` | `lib/floorplan/theme.ts`(`STROKE_SELECTED`/`CANVAS_BG`/`STROKE_ERROR`),与已集中的 `ROOM_COLORS`/`FURN_COLORS` 看齐 |

---

## 按 价值/风险 排序

### P1 — 高价值 + 零/极低风险(纯重构,不动视觉,可放心快做)

- **P1-A 删除 `FloorplanPreview.tsx`**(Q2-#4)。已核实全仓零引用。影响面:单文件删除;**不动任何线上渲染/交互**(组件根本没被挂载)。一次性消除 #4/#9/#10/#3 中各一份重复源。
- **P1-B 抽 `lib/floorplan/fieldStyles.ts`**(Q2-#1 的常量部分):把 `labelCls`/`inputCls` 收为单一来源,两面板 import。影响面:2 文件;**纯字符串提取,DOM 输出零变化**,无需回归。
- **P1-C 抽 `lib/floorplan/theme.ts` 颜色常量**(Q2-#12):把 `#e0701a`/`#0b1437`/`#dc2626` 等替换为常量。影响面:~6 文件;**值不变 → 渲染零变化**,无需回归(替换后 grep 确认无残留字面量即可)。
- **P1-D 复用 `EMPTY_SELECTION` + 抽 `isBackgroundTarget`**(Q2-#11):影响面 1 文件(FloorplanEditor);逻辑等价,降手抖漏字段风险。**轻回归**(确认选中/清空仍正常)。

### P2 — 高/中价值 + 需回归验证(产出新 markup,目标是视觉/行为不变)

> 这批都**会动渲染**,改完需逐项目视:布局像素、暗色、pointer 事件、拖拽命中。

- **P2-A 抽 `StageSvg`/`FloorplanCanvas`**(Q2-#3):EditorStage + FurnitureStage 复用。**动渲染+交互**(pointer 回调/`touch-none`/背景捕获),重点回归拖拽与背景点空清选。
- **P2-B 抽紧凑字段组件 `Field/TextRow/NumberRow/SelectRow`**(Q2-#1 主体):两面板 ~28 处字段行收编。**动渲染**,回归各字段读写(特别是 0 值)、select 选项。顺带补 `htmlFor`/`id`(a11y)。
- **P2-C 抽 `SidePanel`+`PanelSection`**(Q2-#2,`SidePanel` 基于 `Card` → 同时落地 Q1-#1)。**动渲染**(圆角/阴影/border 可能微变),需确认两面板与画布壳观感一致。
- **P2-D 抽 `ToggleButton`+`SegmentedControl`**(Q2-#6)、`SaveButton`/`DangerButton`(Q2-#7)、`StatusLines`(Q2-#8)、`BackendErrorBanner`(Q2-#9)、`LoadStateBadge`(Q2-#10)。一批小展示组件,**动渲染**(应像素等价),回归 active 态/loading 态/禁用态/红绿灰配色。
- **P2-E `RoomRect` 加 `readOnly` + 抽 `RoomsLayer`**(Q2-#5),FurnitureStage 改用之。**动渲染**,回归家具模式下房间参考层的淡显与不可点。
- **P2-F `cut` 改 `Switch`**(Q1-#2):**动交互**(取值从 string→boolean),回归断墙开关读写与保存。

### P3 — 真价值但高风险/大改 或 低增益(单独批次或择机)

- **P3-A 拆 `FloorplanEditor`(960 行)上帝组件** → `useGeometryEditor`/`useFurnitureEditor`/`useToast` + `<GeometryMode>`/`<FurnitureMode>`。价值高(符合 200-400 行规约),但**风险最高**(几乎触碰全部状态/拖拽/深链),必须**独立成批、全量回归**,不要和 P1/P2 混做。
- **P3-B a11y 补强**:Tab 补 `role=tablist/tab/aria-selected`(`FloorplanEditor.tsx:831-850`)、toast 补 `role=status aria-live=polite`、字段 `htmlFor`(可并入 P2-B)。中价值,非阻塞。
- **P3-C tooltip(Q1-#3)/toast 换 Chakra(Q1-#4)**:低增益,现实现可用,择机。
- **P3-D 类型来源统一**:让 `studioApi` re-export `lib/floorplan/types`,Preview 删后此问题大半消失,剩余择机。

---

## 推荐执行批次

1. **批次 0(可立即合,零回归)**:P1-A 删 Preview → P1-B/P1-C 提常量 → P1-D。先删死代码再提常量,后续抽组件的"重复样本"更少更干净。
2. **批次 1(先抽公共组件,后接模板组件)**:先做纯 studio 自有的 P2-A(画布壳)+ P2-B(字段)+ P2-D(按钮/状态/banner 小件)——这批一次性消灭所有面板与画布样板;再做 P2-C(`SidePanel` 基于 `Card`)与 P2-F(`Switch`),即"接入 Horizon 模板组件"的两处,因为它们会引入设计取舍(Card 阴影 vs border、switch vs select),放在公共组件就位后单独验更稳。P2-E 顺手随 P2-A 一起(都在 stage 层)。
3. **批次 2(独立)**:P3-A 拆 god-component,单独 PR + 全量回归。
4. **批次 3(择机)**:P3-B/C/D。

原则:**先删死代码 → 再提常量(零风险)→ 再抽 studio 自有公共组件 → 最后接 Horizon 模板组件(有设计取舍)→ god-component 拆分单独成批**。

---

## 明确「不必改」(避免过度工程)

- **所有 SVG 图元手写**:Horizon 无任何 SVG 等价件,手写是唯一选择,勿引入抽象。
- **原生 `<select>` 取值控件**:Horizon 无表单 Select,`dropdown` 是菜单弹层,**切勿替换**。
- **裸 `<button class="bg-brand-500">`**:Horizon 自身也这么写且无 Button 组件,用的是 brand 主题色阶(非硬编码),合理。
- **勿换 `InputField`**(尺寸不符 + 0 值 bug),用 P2-B 自抽紧凑字段。
- **已拆好的小展示组件**(RoomRect/ResizeHandles/OpeningMarker/FreeWallsLayer/FurnitureItem)粒度恰当,**勿再合并/再拆**。
- **`getGeoPoint` 抽 hook、`deriveSoon` 抽 `useDebouncedDerive`**:目前均单副本且已被两模式正确共用,现在抽属过度工程,**待出现第二处需求(如 Preview 复活要交互)再抽**。
- **`updateG`/`updateFurniture` 不可变更新 + `gRef`/`furnRef` 镜像**:模式正确,符合 immutability,勿动。
- **`lib/floorplan/{coords,geometry,furniture,types}` 分层 + `ROOM_COLORS`/`FURN_COLORS` 集中**:做得好,保留。

相关文件:
- `/Users/yixingzhou/project/grandtianfu/apps/web/src/components/studio/FloorplanPreview.tsx`(删)
- `/Users/yixingzhou/project/grandtianfu/apps/web/src/components/studio/editor/FloorplanEditor.tsx`
- `/Users/yixingzhou/project/grandtianfu/apps/web/src/components/studio/editor/geometry/{GeometrySidePanel.tsx,RoomRect.tsx}`
- `/Users/yixingzhou/project/grandtianfu/apps/web/src/components/studio/editor/furniture/{FurnitureSidePanel.tsx,FurnitureStage.tsx}`
- `/Users/yixingzhou/project/grandtianfu/apps/web/src/components/studio/editor/EditorStage.tsx`
- `/Users/yixingzhou/project/grandtianfu/apps/web/src/lib/floorplan/{coords.ts,geometry.ts,furniture.ts,types.ts}`(+ 新增 `theme.ts`、`fieldStyles.ts`)
- 模板件:`/Users/yixingzhou/project/grandtianfu/apps/web/src/components/{card/index.tsx,switch/index.tsx,tooltip/index.tsx,fields/InputField.tsx(:43 有 0 值 bug,勿用)}`