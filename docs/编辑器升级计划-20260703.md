# 户型/家具编辑器升级计划(已确认)

- 确认日期:2026-07-03;探查:4 路并行(矩形假设/目录扩充/画布UX/机会扫描)+ 3 路补充(门厚/玻璃门/墙面材质),全部实证 file:line
- 诉求:①异形空间 ②家具种类扩充 ③画布充分利用屏幕 ④整体摸底;补充:⑤轴测门过厚 ⑥玻璃门 ⑦墙面实拍材质

## 关键选型(定案)

| 决策 | 选择 | 要点 |
|---|---|---|
| 异形空间 | **merge 组升格为逻辑房间**(零 schema 改动、零迁移,分两期) | 墙派生层已天然支持(cell decomposition,r_foyer+r_live 生产在用);缺口全在下游(scene 并集校验/AI 简报聚合/slice 组裁切/渲染缝);斜墙弧形明确不做 |
| 家具扩充 | 通用兜底 → **声明式基元 m_from_spec** → 手写精修;两批 13+7 类到 45 类 | `/api/catalog` 前后端同源;墙挂类不做;单类型成本从 6-8 处词表降到 catalog 一处 |
| 画布 | **渐进(P1)→ route group 全屏(P4)** 两段式 | (shell)/(canvas) 分组 layout;P1 代码(h-full/fit/折叠/共享视口)全复用进 P4 |
| golden | 「无 merge 数据、默认参数逐字节不变」硬门禁;字节变化集中 P5 一次重冻 | 例外:门厚 bug 已破例提前修(P0,shell 单独重冻一次) |
| 门体系 | `material: wood|glass`(默认 wood 零迁移)+ 门框门扇分离,并入 P5 | 玻璃扇复用窗玻璃配方 #bfe0f088;顺带修 double 门型渲染缺口 |
| 墙面材质 | **A 语义标签(P1)→ C 照片参考图入 edits(P2)**;SVG 直贴否决 | `rooms[].walls:{N|S|E|W:{material,photo_id?}}` 挂房间;photos.json 加 purpose=wall_material;参考图 ≤4 张/次 |

## 阶段与状态

| 阶段 | 内容 | 工作量 | 状态 |
|---|---|---|---|
| P0 | 渲染兜底+词表真源+**门厚修复(DOOR_T 40→4.5px)**+视口双bug+undo落帧+**Playwright冒烟+CI** | 2-3d | ✅ 2026-07-03 上线(PR#9/#10,tag ecd4e46,生产门厚目检通过) |
| P1 | 共享视口/画布全高自动fit/面板折叠/缩放快捷键;**保存即校验(含新增挡门WARN)可点击定位**;编辑器内预览抽屉;真实单位mm/㎡;**墙面材质A** | 6-7.5d | 待做 |
| P2 | `/api/catalog`+前端同源;声明式基元;**第一批13类**(tv/floor_lamp/armchair/ottoman/sideboard/wine_cabinet/side_table/dresser/chest/kids_bed/mirror/shoe_cabinet+rug入目录);家具库抽屉化;拖入默认尺寸真实化;**墙面材质C(purpose照片+多图参考)** | 7-10d | 待做 |
| P3 | 异形一期:geometry merge_groups 工具函数;**scene 并集校验/夹取**(最近part确定性tie-break:距离→面积→id);前端 clampToRoom 并集版;slice 组裁切;**简报聚合**;编辑器组体验(贴合建议并房/整组选中拖动/共享边不描边/单label);L形三点直画 | 8-10d | 待做 |
| P4 | route group (shell)/(canvas);编辑器逃出五层壳,画布100dvh+左浮动工具条+可折叠属性面板+家具库抽屉挂入;窗直插模式;(可选)标尺 | 5-7d | 待做(届时按 P1 反馈定深度) |
| P5 | 异形二期(地板缝消隐/layout并集落位/prompt方位并集化)+**门批次**(门框门扇分离/玻璃门material全链/double修复)+墙厚层高参数化+**golden集中重冻(独立PR人工目检)** | 7.5-10d | 待做 |
| P6 | **底图导入描摹**(上传+透明度+两点比例标定,meta.underlay)+厨卫rooms细分+第二批7类 | 5-6d | 待做 |
| P7 | 可选尾巴:实时校验/实时预览/AI方案幽灵diff/mm双向输入/触控工具条/墙面B spike | 按需 | 不排期 |

## 风险与约束(执行时必守)
- golden:P1-P4 期间 plan2d/shell 字节必须不变(snapshot 测试硬门禁);P5 重冻 PR 不夹带其他改动
- 旧数据兼容:无 merge 数据全链路字节不变;已固化方案渲染不经过新目录条目
- AI 链路:P3 交付说明标注残留清单(落位槽限单part/prompt方位按part→P5);所有改动对无 merge 数据保持确定性输出
- e2e 是 P1/P4 的硬前置(已就位:4条冒烟+CI workflow)

## 随访清单
- rug 升格目录时同步 scene 豁免表;round_chair 补注册
- `.phase0-baseline` 本地重冻团队约定(gitignored,CI 无 pytest)
- 编辑器 double 门型选项已暴露但引擎按 swing 渲染(并入 P5 门批次)
- 探查全文:session workflow wua2vne90 / w8pjm1rgg 输出(scratchpad 会过期,要点已收录本文)
