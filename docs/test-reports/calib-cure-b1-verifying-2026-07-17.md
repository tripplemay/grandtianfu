# calib-cure-b1 验收报告 — verifying-1（2026-07-17）

> 编排形态：fan-out 验收（orchestration-patterns §4，Workflow wf_866744a5-f78）——11 个 completed feature 各由一个**隔离 evaluator subagent**（fresh context，仅给批次名+状态文件路径，无实现叙述）独立验收；FAIL/PARTIAL 自动进对抗复核（本轮**零触发**）；F012（executor:evaluator）由独立 evaluator 并行执行。以下 per-feature 结论为各 subagent 返回值**逐字原样**汇编（铁律 12：编排者机械合并，零改写零筛选零软化）。

## 总判定

| Feature | 判定 |
|---|---|
| F001 | **PASS** |
| F002 | **PASS** |
| F003 | **PASS** |
| F004 | **PASS** |
| F005 | **PASS** |
| F006 | **PASS** |
| F007 | **PASS** |
| F008 | **PASS** |
| F009 | **PASS** |
| F010 | **PASS** |
| F011 | **PASS** |
| F012 (spike) | **BLOCKED**（环境阻塞：本机无 relay/fal key；预算分文未动） |

**汇总：11/11 generator features PASS，0 blocking issue，0 对抗复核触发；F012 BLOCKED（非代码缺陷——真实出图所需 provider key 本机缺失，降级 --dry 已完成全部可完成项）。**

**独立性说明：** 全部结论由隔离 evaluator subagent 基于实物（git show 净 diff / 自行运行的 pytest·tsc·lint·ruff 输出 / 自写对抗探针）得出；编排者未参与任何判定。CI（PR #87）：pytest PASS + smoke PASS。

## 逐 feature 结论（subagent 返回原样）

### F001 — PASS

F001 acceptance 逐条判定：(1) dry_run query 参数实装（apps/api/main.py:1166 `dry_run: str = Query("")`，`is_dry_run` 接受 1|true 大小写/空白容忍）。(2) dry_run=1 不写盘+GEOM_READONLY 仍可用：main.py:1175 `if GEOM_READONLY and not is_dry_run` 403 仅拦真保存；test_dry_run_previews_without_persisting（photos.json 逐字节前后比对+photo entry 无 calibration 键）与 test_dry_run_allowed_under_geom_readonly（dry-run 200/真保存 403）PASS；Evaluator 另写 scratchpad 对抗探针验证边界值（dry_run=0/yes/空/2→真保存族被 403，1/true/TRUE/' true '→预览 200）1 passed。(3) 200 响应 {ok,camera,reprojection_error,quality,wireframe}（main.py:1235-1243）：quality 已由 F003 assess_calibration_quality 真接管（main.py:1153,1223 经 _assess_calibration 适配层，acceptance 允许的"F003 合入后补断言"已兑现——测试断言 q.ok/q.level/q.metrics.reproj_px）；wireframe=[{room_id,floor×4,ceiling×4}] 为 photo.room_id merge 组（axon.merge_group_ids，异常退回本房）每成员 rect×mm_per_px 在 z=0/z=perspective._REAL_CEILING_MM 的 cam.project 投影，常量实证=2700（perspective.py:64），未借 axon 1450——test_dry_run_wireframe_matches_camera_projection 以响应 camera 重建 Camera 手算 r_master 8 点 pytest.approx 相等且天花硬编码 2700。(4) 解算失败仍走既有 400 文案路径（main.py:1203-1204 "标定失败: {exc}"）：test_dry_run_keeps_existing_400_paths（结构校验 400+monkeypatch 强制 raise→400 含"标定失败"）PASS。(5) 真保存路径零回归：acceptance 指定的 test_calibration_endpoint_stores_camera（test_render_real_geometry.py:178）与 test_calibration_stores_binding_and_reprojection_error（:191）HEAD 通过（该文件 31 passed）；全量 api 套件 425 passed 0 failed 0 skip（尾行 "425 passed in 19.04s"）。HEAD 真保存路径相对 F001 时点的新增（quality 快照/BAD_CALIBRATION 400 门/points 模式）系 F003/F004/F008 各自 acceptance 范围内变更且各有 feature commit 归属，非 F001 回归。(6) pytest 同 commit：a211cdd 同 commit 含 apps/api/tests/test_calibration_dry_run.py 6 例，acceptance 四项要求（不落盘/GEOM_READONLY+dry_run=200/wireframe 8 点与 Camera.project 手算一致/merge 组 r_foyer 返回 2 成员 ["r_foyer","r_live"]）全覆盖，6/6 PASS（尾行 "6 passed in 0.11s"）。其他：ruff 对 F001 三涉及文件仅 main.py 1 条 I001，经 a211cdd~1 版本对照跑 ruff 同样报出=既有基线噪声非本 feature 引入；红线目录 git status --short data/ docs/test-reports/ docs/test-cases/ .auto-memory/ 净空（跑测试后复查 data/ 仍净空）；commit tag feat(calib-cure-b1-F001) 映射 features.json F001（铁律 10）；conftest.py 共享文件改动（client_fal 再导出）经全量套件 425 绿证明无收集副作用。F001 acceptance 无 [L2] 浏览器走查项（A 期预览走查属 F002/spec §8.3 用户项），无未执行 L2 项。判定 PASS。

**non-blocking：** 1) dry-run 对不存在的 photo_id 返回 200+空 wireframe（照片/几何查询在降级 try/except 内，main.py:1214-1220），真保存路径对同输入会经 baseline_store 报错——acceptance 未对 dry-run 无效 photo_id 设要求（"未标房间→空线框"降级是 acceptance 认可设计），不阻断，仅记录语义差异供前端知悉。2) main.py 既有 1 条 I001 基线噪声仍在（任务说明已预先声明，非本批引入）。3) building 期遗留 4 个 .claude/worktrees/ agent worktree（a211cdd/2c68994/195c285/ccac4a1），批次收尾时可清理，与 F001 验收无关。

### F002 — PASS

F002 逐条验收（commit 44e2880 + HEAD 实物，F009/F010 迭代后行为未回归）：(1) 预览标定→dry-run→SVG 线框 PASS——studioApi.ts 新增 previewPhotoCalibration（同 payload+?dry_run=1，HEAD L489）；canPreview=线2+2+锚点就绪（PerspectiveCalibrator.tsx:262-270）；CalibrationPreview.tsx:32-83 画地面/天花四边形+竖棱 per merge 成员，fuchsia 虚线+vectorEffect=non-scaling-stroke 与 emerald/brand 输入线区分，挂载照片 SVG 层（L509-514）。(2) 误差数值+评级徽章 PASS——重投影误差 toFixed(1)px + Badge good→green(好)/suspect→amber(可疑)/bad→red(坏)+reasons 列表（CalibrationPreview.tsx:19-26,99-126）；评级由后端 assess 单一真源（perspective.py:586-682：good<25px 无 reasons/硬门>50px→ok=false），前端不复算。(3) 确认门 PASS——canConfirm 要求 preview 非空且 quality.ok（L273-278），bad 禁用+title『标定质量不合格,请按上方原因修正输入后重新预览』（L646-659）；实测 bad 输入真保存被后端 400 BAD_CALIBRATION，前端无后端没有的放行。(4) 锚点≥3 PASS——diff 实证 anchorsReady 从≥2 收紧为≥3 各不相同角（L257-260），进度徽章『地面角 n(需 ≥3 且各不相同)』与按钮提示同步。(5) 输入变更预览失效 PASS——invalidatePreview 接入加线/加锚点/撤销/重来/改角标五处 + inputEpoch 飞行请求竞态守卫（L165-169,192,210,237,246,299-315）。验收项：好/坏预览走查用 §6 fixture 经同一 dry-run 端点数据面实测 ALL OK（好=200 suspect+2成员×4+4点线框+确认门开；798/f4d 生产 payload=400『近竖直』中文文案→banner+门关；env 收紧构造 assess-bad=200 ok:false level:bad+red 徽章+确认门禁用+线框仍返回；各次 dry-run photos.json byte-identical 不落盘）；pytest apps/api/tests/test_calibration_dry_run.py+test_calibration_quality.py 39 passed（尾行『39 passed in 0.91s』）；npx tsc --noEmit exit 0；yarn lint exit 0（7 条 warning 全在既有 useViewport.ts 非本批文件）；prettier --check 三文件『All matched files use Prettier code style!』；D6：复用 Modal/LoadingState/Button/NoticeBanner/Badge（import 实证），F002 新增代码零 bg-*-50，dark: 成对（gray-200/white:10、gray-600/gray-300、amber-600/amber-400、red-600/red-400 等）。评估期间未修改任何产品代码，git status 净空。

**non-blocking：** 1) [L2] 真浏览器预览走查（好/坏标定各一次叠真实照片目检）为 spec §8.3 用户项，本环境无浏览器未执行、不判 FAIL；已用同端点 API 数据面走查+代码审读等价覆盖。2) PerspectiveCalibrator.tsx:433 bg-brand-50（stepHint 容器）为 F002 之前既有代码（3cac6a2 L296 已存在且带 dark:bg-navy-900 配对），非本批引入；如需彻底清除 bg-*-50 建议后续批次顺手处理。3) 好输入合成 fixture 评级为 suspect(amber) 而非 good——因 fixture 相机站玄关拍客厅触发离房软信号（~1.9m），系 spec §D1 裁决的预期行为非缺陷；good→green 路径由 F003 单测 test_assess_good_camera_passes_all_gates 与前端 LEVEL_META 映射（tsc 校验）覆盖。4) text-gray-400 单用无 dark: 配对为项目既有惯例（studio 组件树 28 处先例），不计 D6 违规。

### F003 — PASS

F003 五项 acceptance 全 PASS，验收基于 commit 8640618 净 diff + HEAD 28c2675 实物复核 + 实测。(1) assess_calibration_quality 纯函数（perspective.py:612-693）：硬门 reproj>CALIB_MAX_REPROJ_PX(默认50, _max_reproj_px 调用时读 env, :585/595-601)、相机高 -R^T·t z∉[800,2200]mm(:649-655, calib_heal 零改动)、HFOV∉[35,110]°(:656-664) 任一→ok=false；离房(merge并集)>1500mm 仅 reasons+level=suspect 不 fail(:665-675, test_assess_room_distance_is_soft_signal_not_gate)；metrics 四字段(reproj_px/camera_z_mm/camera_room_dist_mm/hfov_deg)齐全；6 条离线纯单测。(2) InputGateError(ValueError) 携 status+payload（main.py:1658-1668），唯一 except 统一返回（main.py:2840-2842）；STALE_CALIBRATION(:2758-2761)/DEGENERATE_GUIDE(:2823-2832) raise 点迁移后 payload dict 与旧 json.dumps 逐字一致、HTTP 均 409；_INPUT_GATE_CODES_409 登记表与判定段已删（grep 全库仅剩 docstring 提及）；status 随 raise 携带、无表可漏登记。(3) 保存端点 assess 不过→400 {code:BAD_CALIBRATION,reasons,metrics}（main.py:1244-1256; test_save_rejects_bad_quality_with_code）；通过→quality 快照(level/reasons/metrics)与 reprojection_error 并列入库（main.py:1262-1263; test_save_stores_quality_snapshot 断言快照无 ok 键、渲染门现算）。(4) dry-run 接真 assess：dry-run/真保存共用同一 _assess_calibration 结论（main.py:1223-1241）；F001 quality:null 骨架断言已补（test_calibration_dry_run.py:37-42）；bad 仍 200 供前端画线框（test_dry_run_returns_200_with_bad_quality_for_preview）。(5) pytest 同 commit：测试与实现同在 8640618，worktree 复跑该 commit 三文件 47 passed；两负样本按 reasons 断言——实测重放 798 reproj=2353.4px 硬拦、f4d reproj=112.4px+相机高 399.0mm 双硬门齐中（test_production_798_assess_catches_garbage_pose / test_production_f4d_assess_catches_knee_high_camera）；端点层两负样本均被 400 拦（HEAD 由 F004 语义层"近竖直"更早拦截，系 F004 acceptance 明文设计的分层，F003 端点 BAD_CALIBRATION 路径由 env 收紧阈值测试独立钉住）；合成正样本通过（test_assess_good_camera_passes_all_gates level=good + 端点 200 入库）；既有 STALE/DEGENERATE 409 零回归（test_render_real_geometry.py:210-223/747-764，全文件 31 passed）；env 覆盖生效（unit: test_assess_reproj_hard_gate_and_env_escape_hatch 50→100 放行; endpoint: monkeypatch setenv=-1 触 400）。套件证据：api 全量 425 passed 0 skip（19.32s）、引擎 154 passed（0.52s）；ruff perspective.py+两测试文件 All checks passed，main.py 仅 1 条 I001 且实证存在于 F003 之前的 0d211ea（基线噪声）；红线复核：8640618 stat 未触 calibrate() 内核/calib_heal.py/golden/data/，测试跑后 git status 全净（data/ 无写穿）。

**non-blocking：** 1) acceptance 括注「798 亦触相机出房」不成立且无法成立：实测 798 相机离房 474.3mm，低于软信号阈值 1500mm（也低于旧硬门 1000mm）；该括注与同条 acceptance 第(1)项强制的 spec §D1 2026-07-17 裁决版直接矛盾（裁决版明文：798 离房仅 474mm、硬门拦不住它、靠 reproj 2353px 硬拦；两案硬门命中=798:reproj / f4d:reproj+相机高），属裁决前草稿残留措辞，实现与测试跟随裁决版 spec，判定不受影响。2) HEAD 上原 F003 commit 的两条端点负样本测试已被 F004 重构为语义层拦截断言（"近竖直"），F003 的 assess 结论改由求解器+assess 层测试钉住——分层与 F004 acceptance 明文一致（"在 assess 之前的更早反馈"），非覆盖缺口。3) HEAD 的 _assess_calibration 适配层（main.py:1156-1158）叠加了 F010 的 DIRECTION_MISMATCH 追加逻辑，perspective.assess_calibration_quality 本体仍严格 §D1 四检查；F010 部分归其自身验收。4) F003 acceptance 无 [L2] 浏览器走查项，无未执行项。

### F004 — PASS

F004 逐条 acceptance 判定（证据=文件:行 + 测试输出 + 独立 spot-check）：(1)线长≥max(W,H)×5%：main.py:904,911-913；test_semantic_rejects_short_line PASSED。(2)距竖直<15°拒绝用|dx|,|dy|判：main.py:908-916（dx<dy*0.2679=tan15°）；798 生产 payload 实拦"x_lines 第1条…近竖直"（test_save_rejects_production_case_798_at_semantic_layer PASSED + 独立直调 _validate_calibration_payload 复现），f4d y_lines 同拦；边界验证 14°拦/16°过。(3)组内两线近重合拒绝：main.py:917-921+_lines_nearly_coincident（双配对取小，tol=2%对角线）；test_semantic_rejects_coincident_lines_in_group PASSED。(4)锚点距图缘≥2%：main.py:925-930；test_semantic_rejects_anchor_near_image_edge PASSED。(5)像素两两≥2%对角线+世界去重：main.py:931-938；test_semantic_rejects_anchor_pixels_too_close / test_semantic_rejects_duplicate_world_corner PASSED。(6)≥3 且世界不共线：main.py:922-924,939-940，eps=_ANCHOR_MIN_TRIANGLE_MM2=50,000mm²（notes 授权 Generator 定值）；test_save_rejects_two_anchor_payload（断言"≥3"文案）+ test_semantic_rejects_collinear_world_anchors PASSED；独立 spot-check 面积 49,000 拦/51,000 过，边界行为正确。门在端点层：唯一调用点 main.py:1185（set_photo_calibration_ep 保存+dry-run 共用，lines 分支；points 模式走独立 _validate_points_payload）；分支全量 diff（1e7e495...HEAD）calib_heal.py 与 packages/floorplan_core/ 零改动，perspective.py 仅新增 assess 族函数、calibrate() 本体未动；calib_heal.py grep 零引用本校验=重放不经此门。错误文案逐条中文可行动，前端 PerspectiveCalibrator.tsx:114,604-606 submitError banner 原样直显。pytest 同 commit 四项：798 负样本在 assess 之前被近竖直拦（assess 层结论由 test_production_798_assess_catches_garbage_pose 存量重放另行钉住，reproj>1000px）、两锚点 payload 被≥3拦、合规 3 锚点样本通过（订正后 _calib_payload 物理一致构造 reproj=0，test_calibration_endpoint_stores_camera / test_save_stores_quality_snapshot 保存成功）、既有 test_calibration_rejects_too_few_lines（test_render_real_geometry.py:334）零回归——全 PASSED。套件：test_calibration_quality.py+test_render_real_geometry.py 64 passed；apps/api 全量 425 passed 0 skip 0 fail（尾行"425 passed in 17.86s"）。ruff：main.py 仅 1 条 I001 且基线 1e7e495 同样存在（本批零新增）。红线：git status data/ 及测试产物目录净空；commit 82f09b0 仅含 main.py+2 测试文件+状态记账，无夹带；附带 _calib_payload fixture 订正（镜像相机→物理一致构造）系≥3门的必然连带、在 batch_scope 允许路径内且 commit message 已披露。

**non-blocking：** ①结构层保留旧文案"anchors 需 >=2 个"（main.py:889-890）：0/1 锚点时先见">=2"、补到 2 个再被语义层"≥3"拦，文案两段式跳变；正常 UI 流（F002 anchorsReady≥3 才可提交）不暴露，仅 API 直调可见，不影响门禁正确性。②notes 建议的 eps"单测边界"在测试文件中无阈值附近显式用例（现有共线测试用面积=0），本次验收已用独立 spot-check（49k/51k）确认边界行为正确，不构成缺口；建议后续批次顺手补 1 条参数化边界测试。③near-vertical 阈值 0.2679 为 tan15° 的四位截断（对应 14.9997°），偏差可忽略，仅记录。

### F005 — PASS

F005 逐条 acceptance 判定（全部基于实物 diff 与实测）：(1) 门位置与实现 PASS — main.py:2757-2761 stale 门在前 → 2762-2773 畸形载荷 try/except 409 → 2776-2790 assess 质量门 409 → 2813 annotate_boxes 在后，"stale 检查后、annotate 前"成立；存量 cal['camera'] 经 perspective.Camera.from_dict 重建（main.py:2763）+ cal.get("anchors") or []（:2777）；room_rects 取 merge 成员——经 _assess_calibration 适配层（main.py:1139-1152，axon.merge_group_ids），即 F003 单一真源保存400/渲染409/dry-run 三处同门（spec §D1）；409 payload 逐字吻合 acceptance：{ok:false, code:'BAD_CALIBRATION', error:'标定质量不合格（…；…），请重新标定后出图', reasons, metrics}（main.py:2780-2790）。(2) 只查质量不查锚点数 PASS — assess_calibration_quality（perspective.py:612-693）无锚点计数门；repo 测试 test_render_allows_two_anchor_good_legacy_calibration：存量 n=2 好标定 200 出图 + job done + relay.calls==1，绿。(3) 畸形载荷 409 不落 500 PASS — 缺 camera：try/except → 409 '标定载荷损坏或不完整…请重新标定该照片'（main.py:2762-2773），repo 测试 test_render_blocks_malformed_stored_calibration_409_not_500 绿；缺 anchors 有 camera：assess 空锚点 fail-closed（perspective.py:640-642 reproj=None→ok=False'缺有效锚点'）→ 409，Evaluator 探针实测绿。(4) 前端零改动基本交互（acceptance 自declare可选不强制）PASS — F005 commit 099303f diff 仅 main.py(+28)/test(+105)/状态文件，无前端改动；既有 page.tsx:566-567 `msg.includes('标定') → reload()` 覆盖两条 BAD_CALIBRATION 文案（均含"标定"）。(5) pytest 同 commit PASS — §6 负样本注入渲染门：798 端到端注入 repo 测试 test_render_blocks_bad_stored_calibration_409 绿（409+code+重投影reason+relay/fal 零调用"不烧AI预算"断言）；f4d 在 repo 内为组合覆盖（test_production_f4d_assess_catches_knee_high_camera 纯函数重放钉住 ok=False 双硬门 + 798 注入证明门管道），Evaluator 另写隔离探针（scratchpad，未入项目）端到端实测 f4d 存量注入 r_foyer 场景 → 409 BAD_CALIBRATION + 相机高度/重投影双 reason + metrics.camera_z_mm<800 + provider 零调用，2 passed——行为面"两负样本注入被 409 拦"2/2 实测成立；合成好标定照常出图（fal fake provider 设施）：同(2)测试 200 出图绿；STALE 优先级：test_render_stale_takes_priority_over_quality（同时 stale+坏质量 → 报 STALE_CALIBRATION）绿，且门序为代码结构保证。回归与环境：api 套件 425 passed 0 skip（17.83s，含既有 stores_camera/stores_binding/STALE/DEGENERATE 409 家族零回归）+ 引擎套件 154 passed；ruff 对 main.py+test_calibration_quality.py 唯一 1 条 = main.py:9 I001，与批次前基线（970eec3^）逐字相同，确认既有噪声非本批引入；data/projects 红线测试前后 git status --short data/ 均净空（200 出图路径 _wait 排空 job 有效）；commit tag feat(calib-cure-b1-F005) 映射 features.json F005（铁律 10），无夹带文件。

**non-blocking：** 1. acceptance 字面"§6 两负样本存量注入 photos.json fixture 后 render 被 409 拦"中，f4d 的端到端注入 pytest 未入库——repo 采用分层组合覆盖（f4d assess 纯函数重放 test_calibration_quality.py:181-189 钉住结论 + 798 注入 :342-361 证明门管道；:172 docstring 显式声明重放测试承担"F005 渲染门对存量的判定结论"）。Evaluator 已独立端到端实测 f4d 注入 → 409 成立（探针在会话 scratchpad，未入项目）；f4d 两条硬门（相机高/reproj）均不依赖 798 未覆盖的参数路径，端到端增量回归价值边际。若要字面闭环，后续批次补 1 条测试函数即可（照抄 798 注入测试换 _payload_f4d）。2. spec/acceptance 引用行号漂移属正常演进（main.py:2392-2399 现为 2757-2761；page.tsx:541-542 现为 566-567；main.py:2404-2412 现为 2791-2801），实物核实无碍。3. F010 后续 commit 在 F005 门的 _assess_calibration 调用处追加了 direction 参数（main.py:2778）——属 F010 范围（direction 门只拦 >135° 反向），不改变 F005 验收结论；全量套件绿证明两 feature 叠加无冲突。

### F006 — PASS

F006 逐条 acceptance 判定（证据=实物 diff e5b2728 + 自跑测试 + 独立数值复核）：(1) 聚合出画检查 PASS——perspective.py:457-471 include 过滤后逐件跑 box_usability，`(not usable) or in_frame_frac<GUIDE_OFFFRAME_IN_FRAME_FRAC(0.15)` 计 offframe；perspective.py:486-490 `offframe*3>total`(>1/3) 追加『N/M 件家具的标注盒基本不在画面内——相机标定与场景严重不符, 引导图无有效位置信息』；阻断沿用既有 DEGENERATE_GUIDE 409（main.py:2820-2832 InputGateError），F006 diff 零新 code。独立脚本复核 f4d fixture：14 件 skip rug/entry_door 后 total=12、offframe=6（dining_table 0.000/sofa 0.105/sofa 0.000/coffee_table 0.000/media 0.000/curtain 0.148），issue 实测触发 '6/12 件…'。(2) near×不可见矛盾话术禁止 PASS——annotate_boxes legend 增 min_in_frame（perspective.py:556-561，partial 件取各件最小值，unusable 记 0.0）；_geometry_lock_prompt near 话术仅 `entry.get('near') and entry.get('min_in_frame',1.0)>=0.05` 时生成（main.py:2653），否则落 elif partial 降级。实测 f4d 餐桌 legend {partial:True, min_in_frame:0.0, near:True}→prompt 无 'near foreground'、含 'partly outside the frame'（f4d 病灶转为降级 ✓）；可见 near 件（min_in_frame=0.6）'near foreground' 保留（原电视柜修复不回退 ✓）。(3) pytest 同 commit PASS——e5b2728 同 commit 增 4 条测试（test_calibration_quality.py:418-475：test_f4d_guide_sanity_flags_mass_offframe / test_f4d_prompt_downgrades_invisible_near_to_partial / test_near_note_kept_for_visible_near_piece / test_guide_sanity_aggregate_needs_min_items），f4d 生产家具 fixture 纯数值入库；既有 guide_sanity 5 条（test_perspective.py:452-506）零回归；GUIDE_SINGLE_BOX_MAX_FRAME_FRAC=0.9 未动（diff 未触碰 + 边界承重测试断言 ==0.9 通过）。命令输出尾行：`pytest apps/api/tests/test_calibration_quality.py apps/api/tests/test_perspective.py -q` → '75 passed in 1.31s'；全量 `pytest apps/api/tests -q` → '425 passed in 18.70s'（0 skip 0 fail，含 DEGENERATE_GUIDE 渲染 409 既有测试 test_render_real_geometry.py:747-764 与 STALE 家族）；`ruff check` perspective.py 与 test_calibration_quality.py clean，main.py 仅 1 条既有 I001 基线噪声（任务说明已声明非本批引入）；`git status --short data/ docs/test-reports/ docs/test-cases/ .auto-memory/` 净空（红线无写穿）。引擎套件未跑：F006 diff 仅触 apps/api，未碰 packages/floorplan_core。

**non-blocking：** 1) acceptance 文本预估『12 件 5 件 <0.15』，实测 6/12——第 6 件 curtain in_frame=0.148 贴线略低于 0.15；操作性条件『>1/3』成立且测试断言的是聚合 issue 出现而非具体件数，不影响判定。2) GUIDE_OFFFRAME_MIN_ITEMS=3 下限为 acceptance 文本之外的实现细节，但被同 acceptance 的『既有 5 条零回归』子项反向强制（无此下限则 test_guide_sanity_does_not_flag_legit_offframe_clipped_box 的单件合法贴脸盒会被 1/1>1/3 误拦），有专项测试 test_guide_sanity_aggregate_needs_min_items 与注释钉住，属承重设计非放宽。3) min_in_frame 仅在有件出画（partial）时写键，全可见类型不写（legend 字节不变的保守选择）；prompt 门 default 1.0 下行为与字面『各件最小值』数学等价（partial 件存在时 min(partial 件)=min(全件)；全可见时真 min≥0.85 与 default 1.0 在 0.05 门同侧）。4) F006 acceptance 无 [L2] 浏览器走查项，全部为后端/pytest 可验证项，无未执行项。

### F007 — PASS

F007（DELETE 标定端点 + 前端清除入口）逐条 acceptance 全 PASS。证据：(1) baselines.delete_photo_calibration（apps/api/baselines.py:722-745，commit 9d484db）逐行镜像 set_photo_calibration（697-720）：project_lock、_load_baseline_meta 仅校验版本存在不拦 superseded（允许历史版本）、atomic_write_json、photo 不存在 BaselineNotFound→404；无 calibration 键时幂等返回 {ok:true,removed:false} 且不写盘（Evaluator scratchpad 磁盘字节级交叉验证 2 passed）。(2) DELETE /api/projects/{house}/baselines/{version}/photos/{photo_id}/calibration（apps/api/main.py:1325-1337）：GEOM_READONLY 403 与 set 端点逐字同模式，403 路径磁盘字节不变（独立验证）。(3) 前端：page.tsx:886-893 『已标定 ✓』态「清除标定」按钮（Button neutral-outline），onClearCalibration（page.tsx:482-501）复用既有 useConfirm/ConfirmDialog（danger 确认）→ studioApi.deletePhotoCalibration（studioApi.ts DELETE+unwrap）→ toast → await reload()；删后 calibration 空 → 未标定 Badge + L881 回退非几何锁定路径文案。(4) pytest 同 commit：9d484db 含 test_delete_calibration_removes_key_and_falls_back（photos.json 无 calibration 键 + 幂等二删 removed=false + fal.calls==0 同 no_calibration_falls_back 口径）/ test_delete_calibration_geom_readonly_403（403+未删）/ test_delete_calibration_missing_photo_404；实测 test_calibration_quality.py 33 passed（-k delete_calibration 3 passed）、test_render_real_geometry.py 31 passed（参照测试零回归）；912493f（F007 tag，仅测试文件）补 _drain_render_job 排空回退路径后台 job 根治 data/projects 写穿竞态——本机有 rsvg-convert（回退真出图场景活跃），跑后两次 git status --short data/ 均净空实证。(5) tsc exit 0；yarn lint 0 errors（仅既有 useViewport.ts exhaustive-deps warnings，非 F007 文件）；prettier 两触及文件 pass；ruff 仅 main.py/baselines.py 各 1 条既有 I001（git show main 基线同在，非本批引入），测试文件 clean。commit tag 与 features.json F007 映射合规（铁律 10），触及路径全在 batch_scope.allowed_paths 内。

**non-blocking：** 1) [L2] 浏览器实际点击「清除标定」走查（confirm 弹窗→toast→列表回未标定态）无浏览器环境未执行，代码/类型/API 链 L1 已全部核实，列入用户 L2 走查项，不判 FAIL。2) 标定卡未标定态文案（page.tsx:881）仍写「标出两组正交墙线 + 2 个墙角」，与 F002 收紧的 ≥3 锚点门不一致（用户照此只标 2 角会被前端禁存/后端 400）——该文案不属 F007 acceptance（F007 仅要求删后回到该既有回退文案），归 F002/F010 文案域，供 fan-out 汇总方交叉核对。3) 「清除标定」按钮在 calibration_stale（标定已过期）态也显示——acceptance 仅要求『已标定 ✓』态；属合理超集（过期坏标定同样需要自助出口），非偏差。4) 幂等二删与 GEOM_READONLY 403 路径均不写盘（updated_at 不刷），比 acceptance 更严格，为优点记录。

### F008 — PASS

F008 验收 PASS（5/5 条 acceptance 全过）。证据：(1) derive_features — apps/api/aigc/calib_features.py:40-135（纯 numpy，import 仅 numpy/floorplan_core.axon/相对 perspective，不 import main）；merge 组=axon.merge_group_ids 异常退回单成员；开放边界重合角双方剔除（test_derive_features_drops_shared_open_boundary_corners 断言共享角 (0,1000)/(1000,1000) 不出现）；d02 门框 2 点世界坐标=wall.at/span×mm_per_px 精确断言（test_derive_features_merge_members_and_door_jambs，members==["r_foyer","r_live"]，种子 geometry 核实 r_foyer.merge=m_living、d02 存在）；落地窗 wtype=='full' 判定+normal/high 不出点带注释与实证来源（种子 wtype 值域 {full,high,normal} 已核实）；id 稳定（corner:{mid}:{角名}/{kind}:{oid}:{a|b}，sort 后断言有序）。(2) solve_pnp — calib_features.py:141-219：Hartley 归一化 DLT 单应→HFOV 40-110° 41 档扫描+两级细扫→SVD 极分解正交化→物理门（相机 z>0 + 点深度>0）→最小残差档，返回 perspective.Camera；<4 点/z≠0 raise（test_solve_pnp_rejects_degenerate_input）；合成往返单测 <2px+焦距±2%+相机位置<30mm+天花投影钉 r3（test_solve_pnp_recovers_synthetic_camera）；Evaluator 独立复核：12 组随机相机（yaw 0-360°/pitch 2-25°/HFOV 43.7-88.8°/横竖图幅/含 4 组 n=4 最小点数）往返 max_reproj 0.794px 全<2px、相机位置误差≤1.4mm、天花投影≤0.4px（独立脚本，不依赖 Generator 的 _synthetic_cam 单配置）。(3) 端点 — GET calibration-features 返回 {features,room_ids}（test_calibration_features_endpoint 200；无 room_id 400）；POST mode=points 结构校验 ≥4 点（400 测试钉）/world z=0/px 距边≥2% 图幅（严于"图内"，与 F004 语义族一致）+世界去重+共线拦截——Evaluator 对 _validate_points_payload 四分支独立打靶全中；points/lines 合流后共用 assess 门（main.py:1181-1226 合流→1244-1256 not ok→400 BAD_CALIBRATION 同段代码）；存盘含 mode/points/camera/reprojection_error/quality+anchors 镜像（test_points_mode_save_and_render_chain 断言 mode/points×5/anchors×5/reproj<2/quality.level/openings_hash，且 render-real 全链 method==geometry-lock、relay.calls==1 证 F005 复查兼容）；calib_heal 对 points 载荷 skipped_no_inputs 原样透传不崩（test_points_calibration_heal_skips_gracefully；calib_heal.py:27-33 _has_original_inputs 要求 x_lines/y_lines/anchors 全在——points 载荷缺前两者判 False，核实）。(4) binding 指纹 — main.py:1283-1288 points 模式写 binding.openings_hash=_stable_hash(G.openings)；main.py:1033-1037 _calibration_stale_reason 比对→挪门 span+5 后返回含"门窗"的 stale 文案（test_openings_change_marks_points_calibration_stale）。(5) pytest 同 commit — acceptance 点名的 4 组测试全部在 1abe2c8 同 commit 的 apps/api/tests/test_calib_features.py（r_foyer 实数据点池/合成往返/points 保存→render 几何锁定全链 fake provider/heal 不崩），本地实测 13/13 passed；全量 api 套件 425 passed 0 failed 0 skipped（含既有 lines/stale/STALE_CALIBRATION/DEGENERATE_GUIDE 家族零回归）；跑后 git status data/ 净空；ruff 对 calib_features.py+test_calib_features.py clean；F008 后续 commit 对该三文件零改动（git diff 1abe2c8..HEAD 空），main.py 仅 F010 追加不破坏 points 路径（HEAD 实读核实）；commit tag feat(calib-cure-b1-F008) 映射 features.json F008（铁律 10）；diff 全在 batch_scope.allowed_paths，未触碰 packages/floorplan_core/calibrate() 内核/calib_heal.py（红线核实）。

**non-blocking：** 1) derive_features 实现签名为 (G, room_id)，mm_per_px 从 G.meta.mm_per_px 读取（默认 10）而非按 acceptance 括号写法作第三参数——转换行为与数据源等价，非行为差异。2) openings_hash 为 openings 整表哈希而非"所引用 openings"子集哈希——比字面更保守（无关门窗编辑也会触发 stale 重标），代码注释已明示"过失效比漏失效安全"权衡，行为目标（门窗编辑后 stale）达成；若将来嫌过度失效可在后续批次收窄为引用子集。3) "geometry v7 实数据断言"由 repo 种子 data/projects/D/geometry.json（只读 load）+ client_fal 沙箱 D/v1 承载（生产 v7 只读红线不入测试环境）；断言为关系式（wall×mm_per_px）非硬编码生产值，种子 d02 span=[340,430] 与 notes 记录的生产 v7 [360,470] 属数据版本差异，不影响测试效力。4) GET calibration-features 的 photo 不存在→404 分支已实现但无专项单测（非 acceptance 要求项，仅记录）。5) [L2] 特征点模式浏览器端走查属 F009/批次级用户项，不在 F008 acceptance 内，未执行不影响本判定。

### F009 — PASS

F009 逐条 acceptance 全 PASS（实物证据）：(a) 文件拆分与行数——新增 FeaturePointCalibrator.tsx 491 行、CalibrationMiniMap.tsx 227 行（HEAD 含 F010 扩展），PerspectiveCalibrator.tsx 667 行，全部 <800（wc -l 实测）。(b) 模式切换——模态内容顶部『特征点(默认)/专家(线+角)』设计系统 Button 组，默认 features（PerspectiveCalibrator.tsx:383-406，ariaPressed+dataTestId）。(c) 特征点模式——studioApi.ts:503-515 新增 getCalibrationFeatures 与后端 main.py:1299 GET .../calibration-features 路由/响应 shape 逐段匹配；左侧小窗内联 SVG 四要素齐备：merge 成员 rect 轮廓（CalibrationMiniMap.tsx:91-117）+ 成对开口刻痕（L154-167，door_jamb 琥珀/window_floor 天蓝，:a/:b regex 配对与后端 id 格式吻合）+ 特征点圆点 + 待放点 SVG animate 脉冲（L175-195）；照片点击放置标序号点（FeaturePointCalibrator.tsx:385-393）+ LIFO 逐点撤销（L217-221）；≥4 点 350ms 去抖自动 dry-run points 模式（L190-196→previewPhotoCalibration {mode:'points'}），后端链路 test_points_mode_dry_run_previews_without_persist 覆盖；预览复用 F002 共用件 CalibrationPreview.tsx（import CalibrationPreviewPanel/CalibrationWireframeOverlay，非复制实现），canConfirm=preview.quality.ok 同一确认门。(d) V1 边界——grep 无 onMouseMove/onDrag/snap/magnet，仅点击+撤销/跳过/重来。(e) 专家模式零回归——F009 commit 3402252 对 PerspectiveCalibrator 41 插入/0 删除（git show --numstat），HEAD 专家分支两步提交按钮与 canPreview/canConfirm 门完整（L633-652）。(f) <4 点不触发且提示——runPreview L167 与 useEffect L191 双 guard + 引导条『已放 N 点,再放 M 点自动解算预览』+ Badge『特征点 N(需 ≥4)』+ 按钮 title；features<4 时提示切专家模式；后端同界 test_points_mode_rejects_too_few_points(n=3) 佐证。(g) 撤销重放——undo→invalidatePreview(epoch+1 丢弃飞行返回+preview 置空)→currentTarget 自动回指被撤特征→重放，≥4 时自动重预览（代码级验证）。(h) 实测命令：npx tsc --noEmit 0 err；yarn lint 仅既有 useViewport.ts 7 条 react-hooks/exhaustive-deps 基线警告（backlog 既有条目非本批引入）；prettier --check 4 文件『All matched files use Prettier code style!』；pytest test_calib_features.py+test_calibration_dry_run.py 19 passed（0.38s）。(i) r_foyer 走查场景数据面实证：derive_features(D/v1 geometry,'r_foyer') 返回 members=['r_foyer','r_live']（L 形两成员）+ door_jamb 8 点成 4 对（d01/d02/d10/d_kit）、world 为 mm（×mm_per_px）与小窗坐标系同源。(j) D6——复用 Modal/Badge/Button/NoticeBanner/LoadingState；成对 dark:（bg-brand-100+dark:bg-navy-900 等）；F009 新代码 grep 无 bg-*-50；bg-emerald-500 白字序号点与设计系统 buttons.tsx primary/danger 同惯例。(k) 红线：git status 净空，pytest 未写穿 data/projects。

**non-blocking：** 1) [L2] 特征点全流程浏览器走查未执行（隔离环境无浏览器）——已用数据面（derive_features r_foyer 两成员+4 对门刻痕）+ 代码面（小窗渲染/撤销重放/自动解算逻辑逐行核验）替代；与 spec §8.3 一致列为用户走查项，不判 FAIL。2) 专家分支提示框 bg-brand-50（PerspectiveCalibrator.tsx:433）为 3cac6a2 既有代码、F010 重排时原样搬运，非 F009 引入（F009 新代码用 bg-brand-100 避开），且有 dark:bg-navy-900 成对——建议归 F002/F010 验收面顺手复核。3) D v1 geometry 房间无中文 label 时 label_zh 回退 room id（如『r_foyer·东北角』）——F008 容错设计，生产几何带 label.zh 时显示中文，观察项。4) lint 7 条 useViewport 警告为 backlog 既有条目（BL-useviewport），非本批引入。

### F010 — PASS

F010 三条 acceptance 全 PASS（commit b625012，后续 commit 未触碰其交付物）。 (1) 专家模式动态文案 PASS：stepHint 三步改为 acceptance 目标句式『标两条沿【南北】走向墙体(如东墙/西墙)的水平上下沿…小窗中已高亮该走向的墙』（apps/web/src/components/studio/real-render/PerspectiveCalibrator.tsx:350-356）；进度徽章『南北向墙线/东西向墙线』与预览按钮 title 同步改写；CORNER_LABEL 去掉『(左上)』字面（L57-62，下拉渲染 L588），由平面小窗四角伪特征点高亮替代（cornerFeatures+nextCorner 脉冲+usedCorners 打勾，L358-372/441-454）；CalibrationMiniMap 新增 highlightAxis prop，'ns' 高亮 x=X0/X1 两竖边（=南北走向墙）、'ew' 高亮两横边，phase y→ns/x→ew 与 y_lines=东墙类线的既有语义核对一致（CalibrationMiniMap.tsx:118-153）；grep 全标定 UI：『电视墙』『左上』仅存于代码注释、『落地窗』仅存于 F009 特征点类型图例（FeaturePointCalibrator.tsx:54,333，与几何数据联动的术语而非方位假设）、page.tsx『右上角』为布局用语——A3/A9 缺陷字样在指令文案零残留；roomMissing/geo loading/error/!room 四层分支保护，小窗无空引用（PerspectiveCalibrator.tsx:407-454）。 (2) direction 交叉校验 PASS：按 D7 选『映射清晰→拦截』分支且 commit message 注明实证；实证链独立复核三路闭合——①既有 _camera_zone_phrase docstring（main.py:2403-2415）证 k=0 里角={north,west}=NW，_rotate_compass 顺时针推导 v1/v2/v3 里角 SW/SE/NE 与 _VIEW_FORWARDS 四向量逐项一致（main.py:1106-1112）；②BaselinePhotosCard.tsx:27-31 证 v0..v3=轴测旋转 0/90/180/270° 所见即所得缩略图选择器；③决定性数值复核（scratchpad 脚本跑生产两案 spec §6 payload）：798(v3) 解算前向 (+0.607,-0.729)=NE 象限与期望夹角 5°、f4d(v1) (-0.749,+0.663)=SW 夹角 3°，reproj 2353/112px 与 spec 记载一致——映射若搞反此二真实标注样本将呈 ~175° 被误拦，实测几乎重合即映射方向实证成立，且证实『两生产病例坏位姿朝向仍在正确象限、direction 门对其零误拦』。门实现 _direction_mismatch_reason（main.py:1112-1127）：cos<-0.7071 即 >135° 才拦、|n|<0.3 俯拍豁免、None/legacy(N·S·E·W) 不查（宁可不拦）；R[2,:]=相机前向的约定由测试 _synthetic_cam 构造（M=vstack([right,down,fwd])）直接验证；_assess_calibration 追加中文 reason 并 ok=false/level=bad——reasons 为中文文案与 assess_calibration_quality 既有家族风格一致（code 仅顶层 BAD_CALIBRATION，acceptance 的 DIRECTION_MISMATCH 为语义指称）；保存 400/渲染 409/dry-run 三处同门接线证实（main.py:1226 位于 is_dry_run 分支前、main.py:2778）。对应测试 2 个：test_direction_mismatch_only_when_nearly_opposite（正配/两相邻象限不拦/反向拦/None/legacy 共 6 断言，手算夹角 15°/75°/105°/165° 与断言吻合）+ test_save_rejects_direction_opposite_photo（端点 400+code=BAD_CALIBRATION+reasons 含『拍摄视角』）。 (3) 工具链 PASS：pytest test_calibration_quality.py 33 passed；全量 api 套件 425 passed 0 skip（尾行 '425 passed in 17.67s'）；tsc --noEmit exit 0；yarn lint 0 errors（唯一 warning 为批前既有 useViewport.ts:308 exhaustive-deps，该文件最后修改于历史 commit 44ec188 且 backlog 有 BL-useviewport-hook-deps 明文兜底）；prettier --check 两 F010 文件绿；ruff 仅 main.py:9 既有 I001 基线噪声、test 文件 clean。铁律 10：commit tag feat(calib-cure-b1-F010) ↔ features.json F010 成立；改动全部落 batch_scope.allowed_paths 内；git status data/ 净空（红线）。

**non-blocking：** 1. [L2] 用户浏览器走查（书房 r_guest2 场景文案实际显示 + 小窗墙向/角点高亮视效）环境所限未执行，待用户授权/自行走查——spec §8.3 本就列为用户项；代码层已由 grep + 渲染分支静态分析覆盖。2. FeaturePointCalibrator.tsx:54/333（F009 交付面）图例含『落地窗框×地面』『天蓝短线=落地窗』字样：为特征点类型术语（与房间实际几何联动，无落地窗则无该类点/刻痕），非『南墙=落地窗』客厅方位假设，判定不属 A3/A9 缺陷残留；用户走查时如认为图例措辞仍有歧义可另立小改。3. 渲染 409 路径的 direction 门无专项集成测试（经单一真源 _assess_calibration 同函数 + F005 渲染 409 管道既有测试 + 全量套件绿间接覆盖；acceptance 未要求该专项）。4. 工作区存在批外既有未追踪文件 docs/实拍效果图-几何锁定优化方案-20260717.md（非 F010 产物，验收未触碰，提请编排者归置）。5. 引擎套件（154）未在本 feature 验收中重跑：F010 commit 未触碰 packages/floorplan_core（--stat 证实），由批次级验收覆盖。

### F011 — PASS

F011 逐条 acceptance 判定（全部实物取证）：(1) --dry 干跑本地可复现 PASS——合成真值相机+D 种子 geometry/furniture 双场景（r_live 6 件/r_study 5 件），run_ab.py --dry 两遍运行 10 产物 shasum 逐字节一致（"REPRODUCIBLE: byte-identical across two runs"）；l1_guide.py 单场景 CLI 通过（"L1 引导已写 ... (5 件, hfov=65.2°)"，legend 含 partial/near 与产品结构一致）；--dry 分支 settings=None、aigc 包懒加载不触发（run_ab.py:526-534），零 provider 调用。(2) 引导图目检部件成形 PASS——自跑图与入库自证图（docs/test-reports/spike-l1-guide/dry-run/dry_study_L1_guide.png 等）双重目检：书柜端板+多层隔板+顶板（parts3d.py:157-172）、沙发座+靠背+双扶手（parts3d.py:95-120）、餐桌/书桌面板+四腿+desk 挡板、椅座+靠背；平光分面着色+描边+painter 远→近排序（parts3d.py:337）无穿插异常。(3) 产品代码 diff 为零 PASS——git show c4c83e3 --name-only 全集=scripts/spike/×5+docs/test-reports/spike-l1-guide/dry-run/×4 自证 PNG（测试产物目录，batch_scope.allowed_paths 内）；apps/、packages/、配置 0 文件触碰（非 spike/test-reports 路径计数=0）。(4) ruff clean PASS——python3 -m ruff check scripts/spike/ "All checks passed!"，ruff format --check "4 files already formatted"。实现对照：L0 prompt 与 F011 commit 时点产品 _geometry_lock_prompt 逐行一致（AST 提取函数体、去注释/docstring/注解归一化后 74 行=74 行 "VERBATIM at F011 commit time"）；L1 prompt 36 行 diff 全部落在彩盒映射段与颜色指涉替换（box→mockup），rug/墙面带/附着/near/partial/保结构话术逐字保留（run_ab.py:141-217）；_edit_once 两后端调用形态与产品 main.py:2859 逐字对齐（fal 无 size 参数/relay pick_edit_size+settings.model）；"providers.Settings.from_env" 为 spec §D5 简写，产品实符号为 config.get_settings()（config.py:91，唯一 env→Settings 入口），功能等价；硬编码高度表齐备（SOFA 420/800/620、CHAIR 450/900、SHELF_STEP 350、TABLE_TOP_THICK 40、plant 锥台，parts3d.py:31-46），整盒顶经产品 item_top_z_mm 同源（media 550/dining_table 760/bookshelf 2000/curtain 2700=_REAL_CEILING_MM；wine_cabinet 1400 经 catalog z=MAX_TALL_FURNITURE_Z=1400 注入 item.z，catalog.py:31,133），缺 builder 类型回退整盒且 wall_art z0=1000 墙面带（parts3d.py:224）；"1450" 仅出现于禁借警告注释（grep 实证）；importlib 路径加载 perspective/catalog/plan2d_shapes 不改产品包结构（_product.py），复用 _clip_face_near/NEAR_MM/Camera（parts3d.py:233-263）；evaluate_geometry_lock+classify_failures 量化与 relay tokens/fal 像素记账、summary.md 场景×引导×后端×score/fail_reasons/tokens 表+预算合计+rows.json 均实证（run_ab.py:340-497，dry 汇总实物已查）；README.md 记用法/预算口径/PIPL 红线/两臂公平性/诚实边界。

**non-blocking：** 1) 批内时序 skew：spike L0 prompt 为 F011 commit（c4c83e3）时点产品逐字副本；同批后续 F006（e5b2728）给产品 near 话术追加 min_in_frame>=0.05 降级条件（main.py +5 行），spike 副本未含。F011 depends=[] 且 spec §7 波1 编排先于 F006，属编排设计内漂移非实现失误。触发窗口窄（需 near 且该类型 min_in_frame<0.05，且未触 >1/3 聚合退化门或用 --force 越门），触发时 L0 臂将重现 F006 已修的 near×0%可见矛盾话术，"L0=产品现状"轻微失真。建议 F012 执行前由 Generator 回填该条件（run_ab.py:77 一行）或在 F012 报告注明此边界。2) F011 commit 除 scripts/spike/** 外含 docs/test-reports/spike-l1-guide/dry-run/ 四张自证 PNG——属测试产物目录（batch_scope.allowed_paths 明列），"产品代码 diff 为零"主句不受影响。3) 工作区存在批次外未追踪文件 docs/实拍效果图-几何锁定优化方案-20260717.md（非 F011 产物、非本 Evaluator 产生），data/ 等红线目录净空，提请编排者归属处理。4) 本次验证产物（合成相机 fixture、复现输出）均在会话 scratchpad，未写入仓库。5) README 已知简化段（merge 组手工列全成员、STALE/lint/预算门不复刻、blank 场景量化无意义）为明文声明的诚实边界，acceptance 未要求复刻，不构成问题。

## F012 spike — BLOCKED（原样）

relay(OPENAI_API_KEY/OPENAI_BASE_URL)与 fal(FAL_KEY)本机均缺失（七层自查：shell env/launchctl/profile 链/项目 .env/Keychain/CREDENTIALS.md/兄弟项目，生产 .env 按红线不取），真实出图与量化表无法执行，按预案降级 --dry 目检，不判 PASS。已完成全部可完成项：(1)两场景可信标定构造——study_798 assess ok=True suspect reproj 41.5px、wireframe 贴合优秀；living_f4d 为超广角贴角拍摄，针孔模型极限 reproj 126.8px assess=bad 但 wireframe 中央贴合良好，两臂共用同一相机故 A/B 内部公平性不受影响，且该结果实证 F005 渲染硬门将正确拦截此生产病例照片；(2)run_ab.py --dry 两场景×两臂通过，L1 简模形体目检全部成形（书柜端板+7 层隔板、沙发座/靠背/扶手 L 形、餐桌面+四腿 vs L0 纯色平板），guide_sanity 未阻断；(3)两臂 prompt diff 验证公平性成立（仅映射段措辞不同）；(4)发现 curtain 全高不透明板遮死窗户的结构性风险与产品 prompt 既有冠词噪声（"The purple a full-height bookshelf"）；(5)BL-decor-b2-L2-realphoto 观察点 BLOCKED，dry 层确认挂画/窗帘专门话术在位。未执行项已在报告 §9 明示：8-16 图真实出图、auto_check 量化表、出图落位/穿帮目检、BL-decor-b2 实拍验证、go/no-go 最终判定。标定 JSON/scenes 模板/blank 引导图已入库，key 配好后重跑成本约 30 分钟+原授权预算。features.json F012 状态保持 pending（由编排者落盘）。

**go/no-go（初步）：** 干跑证据给出强 go 信号（L1 在引导图层面结构性消解 5 类失败模式中的 4 类：书柜窄条/餐桌缩背景/L 形并张/椅子环绕；两臂公平性机制成立；成本结构不变），但最终 go/no-go 判定 BLOCKED——AI 服从性（简模跟随度/灰体穿帮率）是 spike 核心问题，须真实出图数据（本仓纪律：换引导要重赛）。建议配 key 后按报告 §8 重跑（标定/scenes/工具全就绪），若 L1 的 auto_check score 与落位目检不劣于 L0 且形体显著改善、无系统性灰体穿帮则 go S1-S3；curtain 遮窗副作用为重跑必看点。

**记账：** 出图 0 张；出图 0 张，费用 ¥0.00（用户授权 ~¥20-30 分文未动；relay tokens 0 / fal 0 MP）

**详细报告：** `docs/test-reports/spike-l1-guide-ab-20260717.md`（含 §8 重跑指引与 §9 未执行项清单）

## 状态流转

- 11 个 generator feature：PASS → 保持 completed
- F012：保持 pending，等待用户决策（配 key 重跑 / 押后另批）
- progress.json status：保持 `verifying`（阻塞在用户输入，非 fixing——无代码缺陷可修）
