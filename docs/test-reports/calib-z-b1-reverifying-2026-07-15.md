# calib-z-b1 — 隔离复验报告（reverifying，fix-round 1）

> **Evaluator：** local/evaluator-subagent（fresh context，未继承任何实现过程对话）
> **日期：** 2026-07-15
> **分支：** `calib-z-b1`（未推 main）
> **被验 HEAD：** `916119e`（= F001 `ca96b61` + F002 `910ad7d` + fix-round 1 `ddb5363` / `f9ee326`）
> **对照基线：** `307fb5e`
> **依据：** 修订后 `docs/specs/calib-z-b1-spec.md` §6 + `features.json` F003 acceptance
> **结论：** **PARTIAL → fixing**（F001 PASS；**F002 的 `1537e` 排除决定应撤销** —— 其前提已被本轮实证推翻）

---

## 0. 结论速览

| # | 验收项 | 结论 |
|---|---|---|
| 1 | 11/11 `C_z>0` + 门后候选唯一 | ✅ **PASS** |
| 2 | 投影目检 z=+2700 上 / z=−2700 下 | ✅ **PASS** |
| 3 | `wall_art` 盒回到墙面带 | ✅ **PASS**（真实生产引导图目检） |
| 4 | 主反证：合成真值控制 + 次反证地面不变 | ✅ **PASS**（自建真值 + 300/300 随机扫描） |
| 5 | 只治垂直 + render-fix-b1 未回归 | ✅ **PASS** |
| **5b** | **[必做] `dabcb`/`1537e` 平面移动视觉定论** | ✅ **PASS —— 两条均已定论，且 `1537e` 结论与 fix-round 1 的假设相反** |
| 6 | 下游 false alarm 是否消除 | ⚠️ **未消除**（20/111→9/96、15/111→6/96，仍 `ok:false`）；spec §7 已划界，但**不得表述为"已消除"** |
| 7 | [L2] 真实 AI 出图 | ⏸ **[L2] 未执行**（无 key + 需计费授权） |
| **➕** | **fix-round 1 排除 `1537e` 的决定** | ❌ **BLOCKING（HIGH）—— 前提被推翻，建议撤销排除** |

**F001 的根因判断与修法，在我独立复核下不仅成立，而且比原主张更强**（见 §2.4 的 300/300 随机扫描 + §2.5b 的 `bcc615` 实拍对照）。唯一阻断项是 F002 在 fix-round 1 中新引入的 `1537e` 排除决定。

---

## 1. 取证独立性

| 项 | 做法 |
|---|---|
| 生产数据 | 自行 `scp` 取回 `deploysvr` 的 `photos_{v1,v6,v7}.json` / `geometry.json` / schemes / artifacts / 4 张空房照，**sha256 与生产逐字节一致**；全程只读，收尾复核生产 hash 未变 |
| 回归 fixture 真伪 | **未默认可信** → 自写 `fixture_audit.py` 从我自己的生产副本重新推导，与 `apps/api/tests/fixtures/prod_calibrations.json` 逐字段比对 → **11/11 完全一致、无遗漏、无夹带**，`_source` 属实 |
| 修前/修后对照 | `git worktree` 检出 `307fb5e`，pre-fix / post-fix `perspective.py` 作为两个独立模块**同时加载对跑** |
| 候选枚举 | **不复用产品代码**，自行重写 4 候选枚举（`cands.py`）以便看清每道门的取舍 |
| 合成真值 | **不复用 Generator 的相机**：自建 look-at 构造，**全程不强加 det**，让 det 从物理里掉出来 |
| PII | 空房照与叠加图**只存 scratchpad，绝不入库** |

---

## 2. 逐条验收

### ✅ 1. 11/11 `C_z>0` + 门后候选唯一 — PASS

从**我自己的**生产副本重放全部 11 条（`matrix.py`）：

| baseline | photo | room | n | STORED C_z | PRE-FIX C_z (#候选) | POST-FIX C_z (#候选) |
|---|---|---|---|---|---|---|
| v1 | `bcc615` | r_live | 3 | −2427.1 | −2427.1 (2) | **+2427.1 (1)** |
| v6/v7 | `bcc615` | r_live | 3 | −2266.8 | −2266.8 (1) | **+2266.8 (1)** |
| v6/v7 | `dabcb` | r_master | 2 | +874.3 | +874.3 (2) | **+874.3 (1)** |
| v6/v7 | `1537e` | r_cloak | 2 | +1515.1 | **−1515.1** (2) | **+1515.1 (1)** |
| v6/v7 | `417ae` | r_live | 2 | −1382.2 | −1382.2 (2) | **+1382.2 (1)** |
| v6/v7 | `ae8e5` | r_garden | 2 | −156.0 | −156.0 (2) | **+156.0 (1)** |

- **POST-FIX：11/11 `C_z>0`；11/11 门后存活候选恰好 1 个**；`max|RᵀR−I| = 3.331e-16`。
- **阳性对照成立**：pre-fix 本机重算 **9/11** 相机在地板下。
- **跨机器抛硬币铁证独立复现**：`1537e` 生产存量 `+1515.1`，本机 pre-fix 重算 `−1515.1`。spec §2.1 的 R2 口径注（"存量值计 7 条 / 本机重算 9 条"）**属实**。

### ✅ 2. 投影目检 — PASS

探针取**该照片所属房间 rect 中心**（规避 `testing-env-patterns §7`）：**POST 11/11 正确**；STORED 仅 4/11。

**更强的探针无关判据（本轮新增）—— 相机 DOWN 轴与真实重力的夹角：**

| 条目 | STORED | POST |
|---|---|---|
| `bcc615`×3 / `417ae`×2 / `ae8e5`×2 | **175.2°–179.1°**（相机"下"指向天花板，物理荒谬） | **0.86°–4.78°** ✅ |
| `dabcb`×2 / `1537e`×2 | 4.79° / 1.07°（**垂直本就正确**） | 4.79° / 1.07°（不变） |

### ✅ 3. `wall_art` 回到墙面带 — PASS

用**产品代码本身**（`annotate_boxes`）对真实生产空房照出引导图：

- `417ae`：STORED `wall_art` v=[1008.8..1118.0] —— **与 spec §2.1 记录的病症 v≈1009..1118 逐位复现**；POST v=[722.7..783.4]（H=1536 → 画面纵向中部 = 墙面带）✅
- **实物目检**：修前引导图所有盒**塌在地板上**（只剩底面可见）、无墙面盒；修后盒**由地面向上立起**，`wall_art`（红盒）**落在深色墙面的挂画高度**，窗帘（黄盒）竖立、酒柜（青盒）成高柜。这正是 spec §1 病症的实物消解。

> 方法学备注：我最初用"`wall_art` 顶 v 是否高于地面家具顶 v"作判据 —— **该判据不成立**（两者景深不同，不可比），已弃用，改以 spec 的画面带口径 + 实物目检定论。

### ✅ 4. 主反证：合成真值控制 — PASS（独立自建，未复用其脚本）

我按物理第一性自建真值（`synth2.py`）：先断言 ENU 右手、相机系 (右,下,前) 右手，再由 look-at 导出 `R_true = A·diag(1,−1,1)`。**det 从未被强加**：

```
det(R_true) = -1.000000        <- 独立复现: (East,South,Up) 左手 => 物理相机必 det=-1
PRE  : det=+1.0000  C=(2400.00, 500.00, -1620.00)   max|R-R_true|=1.954e+00   !! 解到地板下
POST : det=-1.0000  C=(2400.00, 500.00, +1620.00)   max|R-R_true|=6.791e-13   RECOVERS TRUTH
```

我自己的手算也确认左手性：`East × South`（在物理系里）`= [0,0,-1] = Down`。

**本轮新增的对抗扫描（`sweep.py`，现有测试没做）—— 300 组随机良态场景：**

```
POST-FIX  recovers truth   : 300/300
POST-FIX  wrong answer     : 0
POST-FIX  spurious raise   : 0        <- 新硬门不会误杀物理合法相机
POST-FIX  survivors != 1   : 0        <- 候选唯一不是只在这 11 条上成立
PRE-FIX   recovers truth   : 0/300
PRE-FIX   exact z-mirror   : 300/300  <- 缺陷是系统性的, 不是偶发
```

**次反证（地面投影 STORED→POST，探针取房间 rect 内 3×3，非锚点）：**

| 条目 | 位移 |
|---|---|
| `417ae`×2 | **4.63e-10 px** ✅ |
| `bcc615`×3 | ≤ **7.28e-10 px** ✅ |
| `ae8e5`×2 | **1.03e-6 px** ⚠️ 见下 |
| `dabcb`×2 | 2.16e+03 px（预期改变） |
| `1537e`×2 | 2.49e+03 px（预期改变） |

> **口径小瑕（不阻断）：** spec §D4 写 `ae8e5` "≤1e-6 px"，我实测 **1.029e-6 px**，字面略超 3%。量级上仍是百万分之一像素 = 不变；`ae8e5` 是 3024×4032 大图且标定本就差（err 178px），属条件数噪声。建议 spec 把该阈值写成"≤1e-5 px"或直接标注实测值，避免后人误判。

### ✅ 5. 只治垂直 + render-fix-b1 未回归 — PASS

- **`417ae` 地面位移 4.63e-10 px** → render-fix-b1 已在生产确认的餐桌落位**完整保留**。
- **两套 pytest（HEAD `916119e`）：356 + 154 全绿，0 skip**。
- **golden 真跑**：`rsvg-convert` 在位，`test_render_snapshot.py` **5 条实际 PASSED**（非 skipif 静默跳过）。
- **阳性对照（关键方法学）**：把本批新增测试拿到 `307fb5e` 的 pre-fix 产品代码上跑 → **11 条变红**，其中行为性失败包括 `test_calibrate_recovers_synthetic_ground_truth_camera` / `..._puts_camera_above_the_floor` / `..._projects_ceiling_up_and_basement_down` / `..._replay_of_every_production_calibration_is_physically_valid` / `..._is_deterministic_not_a_floating_point_coin_flip` / `test_heal_puts_every_production_camera_above_the_floor` 等 → **这些测试是承重的，不是空转**。
- **无测试泄漏**：跑完 356+154 后 `data/projects` 整树 sha256 **未变**（render-fix-b1 R4 坑未复发）。
- **R1 已闭合**：`data/projects/D/schemes/default/renders.json` 在 HEAD 已恢复 `[]`，工作区干净。
- `ruff check` 本批文件 **All checks passed**；`providers.py`/`main.py` 的 2 条 I001 **在基线 `307fb5e` 上同样存在** → "既有噪声"claim **经阳性对照证实属实**。

### ✅ 5b. `dabcb` / `1537e` 平面移动的视觉定论 — **PASS（两条均定论）**

#### 先厘清机理（我独立推出，`ambig.py`）

```
7 条:  R_post = R_stored @ diag(1, 1,-1)   纯 Z 翻转   -> 地面投影不变
4 条:  R_post = R_stored @ diag(1,-1, 1)   纯 Y 水平镜像
```

且这 4 条的**两个锚点恰好都落在镜像面上**：

| 条目 | 锚点世界 Y | C_stored.y | C_post.y | 中点 |
|---|---|---|---|---|
| `dabcb` | 都在 Y=**14100** | 17393.9 | 10806.1 | **14100.0** ✅ |
| `1537e` | 都在 Y=**7600** | 10514.5 | 4685.5 | **7600.0** ✅ |

⇒ 两个候选是**关于用户所点的那面墙互为镜像**，锚点对二者完全等价 ⇒ **任何基于锚点的探针恒得 ~0 位移**（编排者的提醒成立）。

#### `dabcb`（主卧，×2）→ ✅ **POST 正确**

房间盒叠回真实照片（`overlay.py`）：
- **POST**：地面线贴合落地窗地脚、天花线贴合吊顶跌级、竖边落在真实墙角。
- **STORED**：房间盒被画到**落地窗外的半空**（天花线横穿窗户中部、地面线跑到阳台栏杆高度）；且相机 `C=(18171, 17393.9)` 在**建筑外 3294 mm**（envelope Y max=14100）= 20 余层高空 → 物理不可能。

#### `1537e`（衣帽间，×2）→ ✅ **POST 正确 —— 与 fix-round 1 的假设相反**

spec §2.5 现称"两者都解释不了照片左侧那扇景观窗"。**这一点我解开了，而它恰恰是决定性判据。**

照片里有一处**不可混淆**的特征：一扇**落地窗 + 阳台栏杆 + 城市远景**，且其**天花线性风口与跌级吊顶**与 `dabcb`（主卧）照片里的**完全同款** ⇒ 照片远端那个房间**就是主卧**，那扇窗**就是主卧南向落地窗 `w02`（Y=14100, X 12150..18150）**。

两个候选对这扇窗给出**互斥且可证伪**的预测（`sight.py` / `windows.py`）：

```
STORED  C=(14463.3,10514.5,1515.1)  forward=(-0.462,-0.887,0.008)  朝北
        w02 沿光轴深度 = -3500 mm  ->  在相机背后, 不可能出现在照片里
        (STORED 下画面内的窗全是北向小窗; 落在观测窗位置的是 r_study 的
         普通窗 d-5n2y-1150 —— 非落地、1.6m 宽、不可能有阳台) -> 与照片矛盾

POST    C=(14463.3, 4685.5,1515.1)  forward=(-0.462,+0.887,0.008)  朝南
        w02 沿光轴深度 = +8029 mm  ->  在相机前方, 可以出现
        视线追踪 C -> w02 逐墙核对:
          穿 Y= 4850 于 X=14475.3 ; 门 d-lecv-109 跨 X[14250..15150] -> 穿门而过
          穿 Y= 7600 于 X=14675.9 ; 门 d-muzz-109 跨 X[14250..15150] -> 穿门而过
          穿 Y=10200 于 X=14865.5 ; 门 d09        跨 X[14300..15150] -> 穿门而过
        = 三门对齐的 enfilade(套间轴线), 几何上完全成立
```

叠加图上 POST 预测的 `w02` **右缘落在 x≈430，与照片里窗框右缘 x≈428 吻合**。

> **为何 round-1 的"相机落点"探查会指向 STORED：** 它用的是"哪个候选更像隔门取景"的**直觉**判据（STORED 落在 r_master 距衣帽间门 315mm）。但 POST 同样落在合法可站位（r_study 内，且正对 d-lecv-109 门洞），**该判据无法区分**——正如"相机须在房内"已被 `bcc615` 证伪一样，它也是个退化判据。真正能区分的是**照片里那扇窗**，而它只在 POST 下有解。

**⇒ `1537e` 修后正确。目检未显示"修后更差"，反而显示修前物理不可能（STORED 需要透过 3 道墙看见主卧的窗）。spec §6.5b 的"若目检显示修后更差则回退"**不触发**。**

#### 独立管线校验（`bcc615`，n=3、err=0.000、**无歧义对照**）

为排除"我的叠加管线本身有错"，我在唯一无歧义的样本上做了对照：

- **POST**：地面线精确贴合玻璃幕墙地脚，竖边落在真实墙角 ✅
- **STORED**：**天花板（z=+2700）被画到地板下方的前景里**，竖边由锚点朝下延伸 —— 病症在实拍上一目了然。

⇒ det=−1 约定与整个构造在**真实生产数据**上得到独立确认。

### ⚠️ 6. 下游 false alarm — **未消除**（如实记录）

先自证重放保真，再读结论（`geomlock.py`，用生产实物 artifacts）：

| render | 生产记录 | PRE-FIX 我的重放 | POST-FIX 我的重放 |
|---|---|---|---|
| `3dc57abc`(08:14) | ok=false 0.85「新边缘坏块 **20/111**」 | ok=false 0.850「**20/111**」← **逐字复现** | ok=false 0.850「**9/96**」 |
| `c10fd5bd`(08:12) | ok=false 0.85「新边缘坏块 **15/111**」 | ok=false 0.850「**15/111**」← **逐字复现** | ok=false 0.**900**「**6/96**」 |

- 坏块约减半，`c10fd5bd` 分数 0.85→0.90，但 **`ok` 仍为 false，门未过**。
- **诚实边界**：这两张 `out_png` 是模型**看着修前的错引导图**生成的。用修后盒去评判修前图，只能说明"错盒是误报的**部分**成因"。**要判定误报是否真的消除，必须用修后引导图重出一张 = [L2]。**
- 按 spec §6.6 + §7（不改 `evaluate_geometry_lock` 判据）→ **本批已尽其所能，残余待 [L2] 复测后再判是否另立批。本项不得记为"已消除"。**
- fix-round 1 已按 R5 订正文档口径 —— 复核**属实**。

### ⏸ 7. [L2] 真实 AI 出图 — 未执行

无 `OPENAI_API_KEY` 且需用户授权计费 → 按 decor-b2 / render-fix-b1 降级口径。**未执行，如实记 [L2]。**
**建议用户部署后重新生成 1 张 v7 `r_live` 实拍图**，一次闭合两件事：(a) 原始报障（餐桌）的最终确认；(b) §6 的误报是否随修后引导图消除。

---

## 3. 阻断项

### ❌ [HIGH·BLOCKING] fix-round 1 排除 `1537e` 的前提已被推翻，建议撤销排除

`f9ee326` 依 round-1 的"无法定论"把 `1537e`×2 从自愈中排除（spec §2.5 + `--exclude-photo`）。**该决定的事实前提已不成立**（见 §2.5b）：

- spec §2.5 称"两者都解释不了照片左侧那扇景观窗" → **已解开**：那是主卧南向落地窗 `w02`，**只有 POST 解释得了**（STORED 下它在相机背后 −3500mm）。
- spec §2.5 称"修前在 `r_master` 距衣帽间门 315mm = 典型隔门取景"因此指向修前 → 该判据**无区分力**（POST 同样落在合法站位且正对门洞），与已被 `bcc615` 证伪的"相机须在房内"同类。

**排除的实际代价（不是"保持中立"）：**

| | healed（不排除） | excluded（现状） |
|---|---|---|
| 相机物理可实现性 | det=−1 ✅ 可实现 | **det=+1 ❌ 物理不可实现（镜像相机）** |
| 衣帽间家具落位 | 大致正确（受 err 195.7px 影响，精度一般） | **系统性左右镜像**（柜子画到对面墙） |

⇒ **排除 = 主动把一台已知物理不可能的镜像相机留在生产**，且 `heal` 报告会打印"相机在地板下方：0 条"，**读起来像是干净了**（`C_z>0` 对这 2 条本就不是判据，见 spec §2.3）。**healed 严格优于 excluded。**

**建议（需 Planner / 用户裁决）：**
1. **撤销 `1537e` 排除**，11/11 全量自愈（`--exclude-photo` 机制本身实现正确、可保留备用）；
2. `BL-calib-min-3-anchors` **保留**为后续项 —— 它解决的是 `1537e` 的**精度**（err 195.7px），与"方向歧义"已被本轮定论**是两件事**；
3. spec §2.5 依本报告 §2.5b 改写（§2.3/§2.4 经我复核属实，可保留）。

> 若用户仍倾向保守排除，那是**风险偏好选择**（可接受），但文档不得再以"无法定论"为由 —— 应写明"方向已定论为 healed，因该标定精度不合格（err 195.7px）而选择整体重标"。

---

## 4. 给 Generator 的清单

| # | 项 | 要求 |
|---|---|---|
| R6 | **[BLOCKING]** `1537e` 排除决定 | 依 §3 撤销排除、11/11 全量自愈；或经用户明确裁决保留排除但改写理由。**裁决前不得 `--apply`。** |
| R7 | spec §2.5 事实订正 | 按 §2.5b 改写（w02 判据 + enfilade 视线追踪 + STORED 物理不可能） |
| R8 | spec §D4 阈值口径 | `ae8e5` 实测 1.03e-6 px，字面超"≤1e-6"；建议写实测值或放宽到 ≤1e-5 |
| R9 | `heal` 报告可读性（低） | `camera_below_floor_after=0` 对 excluded 的镜像条目有误导性；建议 summarize 增计 `excluded` 与"det=+1 仍在库"计数 |

> **soft-watch（不阻断）：**
> - `ae8e5`(r_garden) POST `C_z=+156mm` + err 178px = 标定质量本就差，与本 bug 无关（维持既有 soft-watch）。
> - `1537e` 即便自愈，err=195.7px 仍不合格；且该照片实为**从书房穿三道门看衣帽间**（衣帽间只从门洞露一角）—— 作为衣帽间的落位底图本就不理想，属输入质量问题 → `BL-calib-min-3-anchors`。

---

## 5. 未采信 / 独立重跑清单（可审计）

| 被主张 | 我的处理 | 结果 |
|---|---|---|
| fixture「sha256 与生产一致」 | 自取生产 + 逐字段重比 | **属实** |
| 「11/11 C_z>0、门后候选唯一」 | 自写重放，不复用其脚本 | **复现** |
| 「合成真值控制」 | 自建不同位姿、不强加 det | **复现**，并加 300/300 随机扫描 |
| 「417ae 地面 2.8e-10 px 不变」 | 自测（探针不同） | 4.63e-10 px，**结论一致** |
| 「ae8e5 ≤1e-6 px」 | 自测 | **1.03e-6 px，字面略超** → R8 |
| 「356+154 全绿、0 skip、golden 实跑」 | 自跑 + 验非 skipif | **属实** |
| 「新增测试承重」 | 建阳性对照（pre-fix 上跑） | **11 条变红，属实** |
| 「providers/main I001 是既有噪声」 | 在基线 `307fb5e` 上复核 | **属实** |
| 「测试不写 data/projects」 | 跑前跑后整树 sha256 | **属实**；R1 亦已闭合 |
| round-1「1537e 无法定论」 | **独立重查 → 定论 POST 正确** | **推翻** → R6 |
| 「误报已减半」 | 生产实物重放 + 先证保真 | **属实，但未消除** |

---

## 6. 生产安全

- 全程**只读**生产；收尾复核 `deploysvr` 的 `photos_{v1,v6,v7}.json` sha256 **与会话开始时一致 = 未被触碰**。
- 迁移脚本在**只读副本沙箱**上实测：dry-run **零写入**（跑前跑后整树 sha256 一致）；`--apply` 后**幂等**（二次运行 9 unchanged / 0 written）；`.bak` 与生产原件**逐字节一致**（回退可用）。
- **F002 `--apply` 尚未在生产执行，且不应在 R6 裁决前执行。**

## 7. 证据留存

scratchpad（PII 照片与叠加图**未入库**）：
`fixture_audit.py`（fixture 真伪）/ `matrix.py`（11 条 pre-vs-post）/ `cands.py`（4 候选逐门枚举）/ `ambig.py`（镜像机理）/ `side.py`（镜像面 vs 房间）/ `synth2.py`（自建合成真值）/ `sweep.py`（300 组随机对抗扫描）/ `probes.py`（垂直探针 + 重力夹角 + 地面不变）/ `overlay.py` `overlay2.py`（房间盒叠实拍）/ `windows.py` `doors.py` `sight.py`（w02 判据 + enfilade 视线追踪）/ `wallart.py`（墙面带 + 真实引导图）/ `geomlock.py`（生产实物误报重放）
