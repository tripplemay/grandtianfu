# calib-z-b1 — 首轮隔离验收报告（verifying）

> **Evaluator：** local/evaluator-subagent（fresh context，未继承实现过程对话）
> **日期：** 2026-07-15
> **分支：** `calib-z-b1`（未推 main）；被验 commit `ca96b61`(F001) / `910ad7d`(F002)；对照基线 `307fb5e`
> **依据：** 修订后 `docs/specs/calib-z-b1-spec.md` §6 + `features.json` F003 acceptance（旧口径未引用）
> **结论：** **PARTIAL → fixing**（F001 PASS；F002 PARTIAL；F003 §6.5b / §6.6 未达成）

---

## 0. 结论速览

| # | 验收项 | 结论 |
|---|---|---|
| 1 | 11/11 生产标定 `C_z>0` + 门后候选唯一 | ✅ **PASS** |
| 2 | 投影目检 z=+2700 上 / z=−2700 下 | ✅ **PASS** |
| 3 | `wall_art` 盒回到墙面带 | ✅ **PASS**（真实生产引导图目检确认） |
| 4 | 主反证：合成真值控制 + 次反证地面不变 | ✅ **PASS**（用**自建**真值相机独立复现） |
| 5 | 反证（只治垂直）+ render-fix-b1 未回归 | ✅ **PASS** |
| **5b** | **[必做] `dabcb`/`1537e` 平面移动视觉定论** | ⚠️ **PARTIAL** — `dabcb`✅确认；**`1537e` 无法定论** |
| **6** | **下游 false alarm 是否消除** | ❌ **未消除**（20/111 → 9/96，仍 `ok:false`） |
| 7 | [L2] 真实 AI 出图 | ⏸ **[L2] 未执行**（无 key + 需计费授权） |
| ➕ | **超范围发现：F002 commit 夹带无归属的 `data/projects` 变更** | ❌ **BLOCKING（MEDIUM）** |

**F001 的根因判断与修法在我独立复核下成立**，且 7 条 z 朝下标定的修复是**不可争议的正确**。阻断项是 F002 的提交卫生、`1537e` 的定论缺口、以及下游误报未消除。

---

## 1. 取证独立性（不采信任何转述）

| 项 | 做法 |
|---|---|
| 生产数据 | 自行 `scp` 取回 `deploysvr` 的 `photos_{v1,v6,v7}.json` / `geometry.json`，**sha256 与生产逐字节一致**（只读，全程未写生产） |
| 回归 fixture 真伪 | **未默认可信**：自写脚本从我自己的生产副本**重新推导**，与 `apps/api/tests/fixtures/prod_calibrations.json` 逐字段比对 → **11/11 完全一致、无遗漏、无夹带**（`_source` 头声明属实） |
| 修前/修后对照 | `git worktree` 检出 `307fb5e`，把 pre-fix / post-fix `perspective.py` 作为两个独立模块同时加载对跑 |
| 解析口径 | 镜像 `main.py::_calibration_camera` 的解析（`anchors[].world/px`），确保重放与生产同路径 |
| 真实照片 | 自行取回 4 张空房照（sha256 与生产一致），**只存 `/tmp`，绝不入库**（PIPL） |

---

## 2. 逐条验收

### ✅ 1. 11/11 `C_z>0` + 门后候选唯一 — PASS

用**我自己的**生产副本（非其 fixture）重放全部 11 条：

| baseline | photo | room | n | PRE-FIX det/C_z | POST-FIX det/C_z | 门后候选 |
|---|---|---|---|---|---|---|
| v1 | `bcc615` | r_live | 3 | +1.000 / **−2427.1** ↓ | −1.000 / **+2427.1** | **1** |
| v6/v7 | `bcc615` | r_live | 3 | +1.000 / **−2266.8** ↓ | −1.000 / **+2266.8** | **1** |
| v6/v7 | `dabcb` | r_master | 2 | +1.000 / +874.3 | −1.000 / +874.3 | **1** |
| v6/v7 | `1537e` | r_cloak | 2 | +1.000 / **−1515.1** ↓ | −1.000 / **+1515.1** | **1** |
| v6/v7 | `417ae` | r_live | 2 | +1.000 / **−1382.2** ↓ | −1.000 / **+1382.2** | **1** |
| v6/v7 | `ae8e5` | r_garden | 2 | +1.000 / **−156.0** ↓ | −1.000 / **+156.0** | **1** |

- **POST-FIX：11/11 `C_z>0`；11/11 门后存活候选恰好 1 个**（浮点抛硬币根因确已除）；`R` 精确正交 `max|RᵀR−I| = 3.331e-16`。
- **阳性对照成立**：pre-fix 确实带病（本机重算 **9/11** 相机在地板下）。

> **订正记录（非缺陷，但口径需修）：** spec/审计称修前"**7** 条 z 朝下"，那是**生产存量值**的口径。**本机重算 pre-fix 得 9/11 朝下** —— `1537e`(×2) 本机算出 `C_z=−1515.1`，而生产存量是 `+1515.1`。**这恰恰独立坐实了审计自己的"同输入换机器即得相反 z"抛硬币铁证**（我未采信其结论，是独立复现出来的）。文档写"7 条"时应标明"按存量值计"。

### ✅ 2. 投影目检 — PASS

探针取**该照片所属房间 rect 的中心**（规避 `testing-env-patterns §7` 退化位置坑）：

- **POST-FIX：11/11 `UP-ok`**（z=+2700 投画面上部、z=−2700 投下部）；STORED：7 条 `INVERTED`。
- **更强的独立判据（本次新增）：相机 DOWN 轴与重力的夹角**——比 spec 的探针法更不依赖探针选点：

| 条目 | STORED DOWN vs 重力 | POST-FIX |
|---|---|---|
| `bcc615`×3 / `417ae`×2 / `ae8e5`×2 | **175.2° ~ 179.1°**（相机"下"指向**天花板**，物理荒谬） | **0.86° ~ 4.78°** ✅ |
| `dabcb`×2 / `1537e`×2 | 4.79° / 1.07°（**垂直本就正确**） | 4.79° / 1.07° |

### ✅ 3. `wall_art` 回到墙面带 — PASS

5/5 张照片：POST-FIX 的 `wall_art` 盒 v 区间**严格高于**同位置地面盒；PRE-FIX 4/5 落在地面带或以下。

**并已用真实生产引导图目检确认**（`annotate_boxes` 实跑，非推断）：修前所有盒**塌在地板上**、`wall_art` 无墙面盒；修后盒**由地面向上立起**，`wall_art`（红盒）**落在深色墙面上**。这正是 spec §1 所述病症的实物消解。

### ✅ 4. 主反证：合成真值控制 — PASS（**独立自建，未复用其脚本**）

我按**物理第一性**自建真值相机（在 ENU 右手系里取 right/down/forward，再映射回 (E,S,U)，**全程不强加 det**），位姿也与其审计不同（`C=(−3500,−3000,+1500)`, yaw 42°, pitch 14°）：

```
det(R_true) = -1.000000     <- 独立复现：(East,South,Up) 左手系 => 物理相机必 det=-1
PRE  : det=+1.0000  C=(-3500,-3000, -1500)  max|R-R_true|=1.941e+00   !! 相机被解到地板下
POST : det=-1.0000  C=(-3500,-3000, +1500)  max|R-R_true|=5.363e-13   RECOVERS TRUTH
```

**审计 §2.2 的左手系论点在我的独立构造下成立**（我自己验算：East×South = Down；相机系 (右,下,前) 右手 ⟹ world→camera 必 det=−1）。**D1 的修法方向正确。**

次反证（地面投影 PRE→POST，房间 rect 内 3×3 探针）：`bcc615` / `417ae` / `ae8e5` 均 **0.000e+00 px（精确零）**。

### ✅ 5. 只治垂直 + render-fix-b1 未回归 — PASS

- **`417ae` 地面位移 PRE→POST = 0.000e+00 px（精确零）** → render-fix-b1 已在生产确认的餐桌落位**完整保留**。
- **两套 pytest：353 + 154 全绿，0 skip**。
- **golden 真跑**：`rsvg-convert` 在位，`test_render_snapshot.py` **5 条实际 PASSED**（非 skipif 静默跳过）。
- **阳性对照（本轮关键方法学）**：把本批新增测试拿到 `307fb5e` 的 pre-fix 产品代码上跑 → **6 条变红**（4 条是行为性失败：合成真值 / 相机在地板上方 / 天花板朝上 / 无解 raise；2 条因 `_solve_poses` 同 commit 提取而结构性失败）。→ 这些测试**是承重的**，不是空转。
- **无测试泄漏**：跑完整套后 `data/projects` 整树 sha256 **未变**（render-fix-b1 R4 坑未复发）。
- `ruff check` 本批文件 **All checks passed**；`ruff format --diff` 的差异经**基线对照**证实为本机 ruff 版本噪声（基线 `307fb5e` 同样"需重排"），非本批引入。

### ⚠️ 5b. [必做] `dabcb` / `1537e` 平面移动的视觉定论 — **PARTIAL**

**先厘清一个 spec / 审计都未写明的结构事实（我独立推出）：** 这 4 条的两个候选**不是** z 翻转关系，而是

```
R_post = R_stored @ diag(1,-1,1)      <- 世界 Y 轴镜像（水平），非 Z
（对照：7 条 z 朝下的是 R_post = R_stored @ diag(1,1,-1)，纯 Z 翻转）
```

且 `dabcb`/`1537e` 的**两个锚点共线于同一条房间边**（都在 Y=常数 上）⟹ 翻 Y 的镜像可被 `t` 完全吸收 ⟹ 对锚点重投影**严格等价**。这就是平局的机理，也意味着**任何基于锚点的探针恒得 ~0 位移**（编排者的提醒成立）。**这 4 条的垂直方向本就正确（DOWN 距重力 1.07°/4.79°），本批对它们做的是水平镜像翻转，`C_z>0` 门对它们不构成判据** —— 它"生效"只是因为新构造式强制 det=−1 后，stored 对应的候选 `C_z` 变负而被排除。

#### `dabcb`（主卧，×2 baseline）→ ✅ **确认 POST-FIX 正确**

真实照片叠加（房间盒 = 地面 z=0 + 天花 z=2700 + 竖边，近平面裁剪）：

- **POST-FIX**：地面线**精确贴合窗墙的落地窗/地板交界**；天花线**精确贴合吊顶跌级线**；竖边**精确落在窗墙与右侧实墙的真实墙角**。
- **STORED**：房间盒被画到**落地窗之外的阳台/半空**；且相机中心 `(18171, 17394)` 在**建筑外 3294 mm**（20 余层高空）—— 物理不可能。
- 建筑轮廓 bbox（由 v7 全量房间 rect 推出）：x[0..18150] y[0..14100]；POST-FIX 相机 `(18171, 10806)` 仅**外溢 21 mm** = 贴东墙站位，合理。

→ **`dabcb` 的 1556 px 平面移动是真修复，视觉定论支持本批。**

#### `1537e`（衣帽间，×2 baseline）→ ❌ **无法定论（inconclusive）**

我做了 4 组独立探查，**证据互相矛盾**：

| 探查 | 指向 |
|---|---|
| 理论（左手系 ⟹ det=−1；构造已由合成真值 + `bcc615` n=3 无歧义对照双重验证） | **POST-FIX** |
| 房间盒叠加真实照片 | **STORED** 更贴合（右墙地脚线 / 右墙天花交界 / 墙角竖边三者同时吻合）⚠ 但 STORED 的两条边是**跨近平面被裁剪**后的线段，"吻合"可能是裁剪巧合 |
| 相机落点（建筑内可站位；"相机须在房内"已被 `bcc615` 证伪，故只用"建筑内"这条未被证伪的弱判据） | **STORED**（落在 `r_master` 主卧、距衣帽间门 315 mm = 典型隔门取景）<br>POST-FIX 落在 `r_study`(次卧)，与衣帽间隔数道墙 |
| 邻室布局投影（把 `r_live`/`r_mbath`/`r_master` 一并投上） | **两者都解释不了**照片中部偏左那扇城市景观窗：两个候选的相机 right 轴都指向西（−0.887），⟹ `r_live` 都落在画面右侧 |

**且 `1537e` 的锚点重投影误差 = 195.7 px**（11 条里第二差，仅次于已被登记 soft-watch 的 `ae8e5` 178 px；对比 `bcc615` 0.000 / `dabcb` 9.86）。**最后一项探查提示：`1537e` 的标定输入本身可能就不自洽**（锚点点错角 / 墙线分组错 / room_id 归属错），此时"两个候选哪个对"是个**伪命题** —— 都不对。

**诚实结论：** 我**不能**给出 spec §6.5b 要求的视觉定论。目检**未显示"修后更差"**（故不按 spec 触发"回退该条"），但也**未能确认修后更好**。这与审计 §6.1 自陈的"缺独立视觉确认"是同一个缺口 —— **它没有被本批关闭**。

**建议（择一，需用户拍板）：**
- **(A) 稳妥（推荐）**：F002 `--apply` 时**排除 `1537e`(×2)**，改为请用户按 `BL-calib-min-3-anchors` 用 **≥3 个不共线锚点重标衣帽间** —— 其 err=195.7 px 本就说明该标定质量不合格，重标同时解决歧义与精度；
- **(B)** 凭已验证的构造（理论 + 合成真值 + `bcc615` n=3 对照 + `dabcb` 实物）整体接受 11/11，把 `1537e` 记 soft-watch，上线后人工看一张衣帽间实拍图闭环。

### ❌ 6. 下游 false alarm 是否消除 — **未消除**

用**生产实物**重放（生产 render `3dc57abc`，2026-07-15T08:14，`417ae`/`r_live`）：

```
生产记录:            ok=false score=0.85  "盒区外出现新结构 (新边缘坏块 20/111)"
PRE-FIX  我的重放:   ok=false score=0.85  "盒区外出现新结构 (新边缘坏块 20/111)"   <- 与生产逐字复现 = 重放保真
POST-FIX 我的重放:   ok=false score=0.85  "盒区外出现新结构 (新边缘坏块  9/96)"    <- 减半但仍报
                     lost_edge_frac 0.064 -> 0.076
```

- **重放保真度先自证**（pre-fix 逐字复现生产 verdict），再下结论 —— 非"跑完没报错"式结论。
- **修后坏块 20/111 (18.0%) → 9/96 (9.4%)，约减半，但 `ok` 仍为 false，门未过。**
- **诚实边界：** 这张 `out_png` 是模型**看着修前的错引导图**生成的（画被挂到墙上、而盒在地板）。用修后盒去评判修前图，只能说明"错盒是误报的**部分**成因"。**要判定误报是否真的消除，必须用修后引导图重出一张 = [L2]**。
- **是否属本批范围：** 按 spec §6.6「若仍报，如实记录并判断是否属本批范围」+ §7「不改 `evaluate_geometry_lock` 判据；若根因在验收器本身，另立批」 → **本批已尽其所能（盒几何已修正且减半误报），残余部分需 [L2] 复测后再判是否另立批。** 本项**不应记为"已消除"**。

### ⏸ 7. [L2] 真实 AI 出图 — 未执行

无 `OPENAI_API_KEY` 且需用户授权计费 → 按 decor-b2 / render-fix-b1 降级口径处理。**未执行，如实记 [L2]。**
**建议用户在部署后重新生成 1 张 v7 `r_live` 实拍图**，同时闭合两件事：(a) 原始报障（餐桌）的最终确认；(b) 上面 §6 的误报是否随修后引导图消除。

---

## 3. 超范围发现（BLOCKING）

### ❌ [MEDIUM·BLOCKING] F002 commit `910ad7d` 夹带无 feature 归属的 `data/projects` 变更

```
commit 910ad7d 变更清单里含：
  data/projects/D/schemes/default/renders.json | 2 +-
  内容: []  ->  2 条 real-photo render 记录 (created_at 2026-07-11, baseline v1)
```

- **与本批毫无关系**：F002 是标定自愈，`renders.json` 不在其范围内 → 违反**铁律 10**（无 feature 号归属的代码/数据修改 = 越界）。
- **与该 commit 自己的 message 直接矛盾**：message 写「全程纯内存 → **结构上不可能写穿 git-tracked data/projects**（render-fix-b1 R4 教训）」，而这个 commit 本身就改了 `data/projects`。
- **不是本批的活泄漏**（我已排除）：跑完 353+154 全套后 `data/projects` 整树 sha256 未变 → 测试没写它。判定为**工作区早已脏的文件被 `git add` 顺手扫入**（内容时间戳 2026-07-11，早于本批）。
- **内容不来自生产**：生产同名文件 27590 bytes 且内容不同（仓库版 5680 bytes）→ 是本地跑 API 产生的记录污染了**种子快照**。
- **影响面**：限于仓库 seed（CI 不会用它覆盖生产），故非 HIGH；但污染 git-tracked 基线数据、且踩了项目明文红线（`data/projects/` 是种子快照，测试绝不可写入）。

**要求：** 从 F002 提交中**剔除该文件的改动**（`git checkout 307fb5e -- data/projects/D/schemes/default/renders.json` 后修补提交），并在推送前执行 `git status --short data/projects/` 确认干净。

---

## 4. 给 Generator 的修复清单（fixing）

| # | 项 | 要求 |
|---|---|---|
| R1 | **[BLOCKING]** F002 commit 夹带 `data/projects/D/schemes/default/renders.json` | 剔除该改动，恢复为 `[]`；推送前 `git status --short data/projects/` 必须干净 |
| R2 | 文档口径 | spec §2.1 / 审计 §5.2 的「7 条 z 朝下」需标明是**按生产存量值**计；本机重算 pre-fix 为 **9/11**（`1537e` 跨机器翻转）—— 该差异恰是抛硬币铁证，建议写入而非隐去 |
| R3 | 文档缺口 | spec / 审计未写明 `dabcb`/`1537e` 的候选关系是 **`diag(1,-1,1)` 水平镜像（非 z 翻转）**、且其**垂直方向本就正确**、`C_z>0` 门对其**不是**直接判据（只是经 det=−1 构造间接排除）。这点对读者理解"为何这 4 条也要改"是承重的，建议补进 spec §2.3 |
| R4 | `1537e` 定论缺口（**需用户拍板**） | 见 §2.5b 建议 (A)/(B)；**在用户裁决前不得对 `1537e` 执行 `--apply`** |
| R5 | §6 误报未消除 | 不要求本批修（spec §7 已划界）；但**不得在任何文档/签收中表述为"误报已消除"**；须记为「盒几何已修正，误报由 20/111 降至 9/96，是否消除待 [L2] 复测」 |

> **soft-watch（不阻断）：** `ae8e5`(r_garden) 修后 `C_z=+156 mm` + err 178 px；我复核确认其 `wall_art` 盒 POST-FIX 投到 v=[−9311..−5835]（远离画幅）——该标定质量本就不可用，与本 bug 无关，维持审计的 soft-watch 判断。

---

## 5. 未采信 / 独立重跑的项目（可审计）

| 被主张 | 我的处理 |
|---|---|
| fixture「sha256 与生产一致」 | **未默认可信** → 自取生产 + 逐字段重比 → 属实 |
| 审计「11/11 C_z>0、门后候选唯一」 | **未复用其脚本** → 自写重放 → 复现 |
| 审计「合成真值控制」 | **未复用其相机** → 自建不同位姿的真值相机、不强加 det → 复现且独立佐证左手系 |
| 审计「417ae 地面 2.8e-10 px 不变」 | 自测 = **0.000e+00 px**（探针不同，结论一致） |
| commit「353+154 全绿、0 skip、golden 实跑」 | 自跑复现；并额外验 golden **非 skipif 跳过** |
| commit「新增测试是承重的」 | **建阳性对照** → pre-fix 上 6 条变红 → 属实 |
| commit「纯内存、不可能写穿 data/projects」 | **建阳性对照**（跑前跑后整树 sha256） → 测试确实不写；**但 commit 自身写了** → 见 §3 |
| 审计「7 条 z 朝下」 | 自测得 **9/11**（口径差异）→ 见 R2 |
| 审计「dabcb/1537e 是镜像解、须目检」 | `dabcb` 目检**确认**；`1537e` **无法定论** → 见 §2.5b |

---

## 6. 证据留存

- 生产只读副本 + 叠加图：`/tmp/eval-calib/`（照片属 PIPL 敏感，**未入库**）
- 关键脚本：`verify_fixture.py`（fixture 真伪）/ `matrix.py`（11 条 pre-vs-post）/ `axes.py`（相机轴 vs 重力）/ `envelope.py`（相机落点 vs 建筑轮廓）/ `remaining.py`（wall_art 带 + 自建合成真值 + 地面不变）/ `geomlock.py`（生产实物误报重放）/ `roombox.py` `layout.py` `fullsize.py`（目检叠加图）
- 生产实物：render `3dc57abc`(08:14) 及其 base/photo artifact；`photos_{v1,v6,v7}.json`；`geometry.json`
