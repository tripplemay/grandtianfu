# 阅天府 Backlog 核对订正版（2026-07-08）

> 本文是对「2026-07-08 backlog 梳理」的**逐项代码核对订正版**。方法：40 项候选逐条对照当前 `main` 代码（Read + Grep + ls），判定 DONE / PARTIAL / OPEN，附 file/commit 证据。
>
> **为什么要订正**：首轮梳理读的是记忆笔记与 `docs/` 报告，落后于代码。最大误源是 `docs/画布能力差距分析.md` —— 它与 `canvas-S01`(`3953aa3`) **同一 commit 提交**，本质是一份「现状 ❌ + 路线」的**开工前规划报告**，其列的缺口当天即被 `canvas-S01→S5b` 系列（2026-06-28）实现。首轮把该文档的「现状(缺口)」误当成了待办。

---

## A. 应从 backlog 划掉：其实已完整实现（6 项）

| 项 | 曾被误列 | 代码实际（证据） |
|---|---|---|
| 画布 **undo/redo + 键盘 + 防丢失守卫** | 「全仓零 onKeyDown、无历史栈、无 beforeunload」 | `useEditorHistory.ts` past/future 双栈；Ctrl+Z/Shift+Z/Y；dirty+beforeunload 拦截；切页 key 重挂载重置栈。commit `c105d0b` canvas-S2 |
| 画布 **视口缩放/平移 + 移动端捏合** | 「viewBox 静态、无缩放平移捏合」 | `useViewport.ts` 光标锚点滚轮缩放 + 双指 pinch + fitBox/reset100（scale 0.2–12）；`ZoomControls.tsx` Fit/100%/缩放%。commit `3953aa3` canvas-S01 |
| 画布 **对象编辑 + 稳定 id + 多选 + 家具库** | 「四向无旋转、下标 key、无多选、单下拉 27 类」 | 自由旋转柄(15°吸附/Shift自由) + 缩放手柄(`computeFurnResize`) + 稳定 id 作 key + Shift/框选/全选/群移/对齐分布(`MarqueeLayer`) + 分类+搜索+SVG缩略图家具库(`FurnitureLibrary`/`FurnThumb`)。commit `ca7505f` S5a + `e194970` S5b |
| **furnish AI 状态 + 预算预检 + 放宽超时** | 「无门禁、90s 偏紧」 | `reserve_furnish()` 原子计次闸（`furnish_daily_cap`，超限 402）；`/api/ai/status`；`request_timeout_s` 默认 **300s**。`main.py:1323` `budget.py:92` `config.py:76` |
| **跨版本方案迁移 CTA（v2）** | 「可能不完整」 | 端到端：`POST /api/projects/{house}/schemes/{scheme_id}/migrate` → `migrate_scheme`(source=migrated/绑 target_baseline/校验)；`studioApi.migrateScheme`；空态与历史列表两处专门 CTA + migrated 徽标。`main.py:847` `schemes.py:617` |
| **链路审计 P2 残留清理** | 「未清残留」 | P1-8 prompt 用归一化坐标(`_prompt_items_from_axon` 取 `_dx/_dy`)；P2-6 furnish model/警告落 meta + `catalog_rev`（`test_furnish_api` 断言）；P2-3 renders 读侧瘦身剥 manifest/usage/prompt。 |

> **系统性订正**：整个「编辑器画布能力」主题在首轮被写成「四阶段均未落地」，实际由 **canvas-S 系列（S01/S2/S3/S5a/S5b，2026-06-28）** 全部实现。`docs/画布能力差距分析.md` 应标注为**已被 canvas-S 系列执行完毕的历史规划文档**，不得再当作 live backlog。

---

## B. 应降级为「收尾小项」：已大部分完成，只剩一小段（7 项）

| 项 | 已完成部分 | 仅剩 |
|---|---|---|
| **结构化 Prompt Compiler** | `compile_brief` 已是 10 字段固定序 + 边界校验的确定性 schema，**已贯通轴测(第7步)+实拍(第5步)两链** | lighting 维度仍硬编码(`prompt_gen.py:217`)；must_keep 只有 `keep_hardscape` 布尔近似；schema 扁平非分层 |
| **实时尺寸/吸附辅助线 + 命中区 + 性能** | 红色对齐虚线(`GuideLayer`)、尺寸 HUD(`dim-hud`)、全层 `React.memo`（commit `2464c98` canvas-S3） | resize/rotate 把手命中区可再加宽（现≈可见 12px/scale） |
| **物理参考图替换** | **墙面材质照**通道完整接入第7步（`purpose=wall_material`→`_resolve_wall_material_photos`→注入 `provider.edit`→`wall_photo_ids` 溯源，带测试） | 「整间实景样板房」作为独立风格参考槽（当前只到墙面材质级） |
| **长 AI 任务体验** | elapsed 计时（三页）+ 三页「停止等待」取消按钮（`cancelRef`） | 后端真 job abort（现任务照跑完进历史）；进度%；离页续轮询；项目级可恢复（现进程内单 job_id，重启即丢） |
| **P6 剩余** | 底图描摹(`underlay`) + 第二批 7 类家具（`bunk_bed/crib/desk_chair/bar_stool/console_table/coat_rack/bidet`，CATALOG=46，`test_p6_furniture`） | 厨卫 rooms 细分（拆 `wet→kitchen/bath`）——刻意绑 golden 重冻窗口 |
| **家具目录补齐** | 已扩至 46 类；`CATALOG_UNKNOWN_TYPE`+通用盒是**刻意保留**的可见性安全网（非缺口） | 按需渐进补类，无硬缺口 |
| **异地备份** | `backup.sh` 完整脚本 + `restore-test.sh` 恢复演练 + systemd 每 6h timer + ExecStartPost 自动挂恢复演练 | 配 `BACKUP_REMOTE` 真实桶凭据 + 跑一次真恢复并记 RTO（**运维态**，代码查不出） |

---

## C. 真实未做 backlog（30 项，按主题 + 可动手程度）

图例：🟢 现在就能动（范围收敛、无产品依赖）｜🟡 需产品先拍板｜🔒 闸门（需人工目检）｜⚪ 常规工程项

### 1 实拍照片接入
- ⚪ **真实工地空房照接入 + 第7步泛化复测**（`P1`）—— `D/baselines/v1` 无 `photos.json`，登记入口(`upload_baseline_photo`)已就位但无真实数据。**外部依赖：需你提供现场照片**。
- ⚪ **水印/人物/遮挡检测门禁**（`P2`，合规）—— `assess_quality` 只做 Pillow 统计 5 类，模型侧检测未做。

### 2 AIGC 提示词与出图质量
- ⚪ **软装层结构化补全**（窗帘/灯具/挂画/绿植/摆件，`P1`）—— 仅 rug 已 per-room 软装层，其余仍自由文本。
- 🟢 **image2 回归评测集常态化运营**（`P2`）—— harness 已建，缺定期真跑 + 人工评分回填 + 历史对比（无 CI/cron）。
- ⚪ **图像 provider 多厂商**（fal/Gemini）—— `get_provider` 只实现 OpenAI，其余抛 `ProviderError`。

### 3 双数据源收敛与命名统一（建议合批，churn 大、收益内部整洁）
- ⚪ **default/root furniture.json 深层收敛**（`P2`）—— 写镜像 R1/R5 已下线，读兜底 R2 / legacy RenderLog 账本 R3 / 双规范化 R4 仍存活。
- ⚪ **参数/字段命名统一**（`P2`）—— API 层 `house`（219 处）vs store 层 `project_id`，逐处桥接，别名未收敛。

### 4 编辑器（画布主体已完成，剩余为诊断/素材/边界）
- ⚪ **家具编辑器面向出图诊断**（`P2`）—— 挡门 WARN 已有，靠窗/缺核心家具/动线拥堵三类诊断未加。
- 🟡 **合并组悬空 cut 洞自动降级定案**—— 并房悬空门洞引擎 D12 报 ERROR 拒存，自动降级 vs 保留手动**需产品定**。
- ⚪ **家具库风格变体基元扩充**（`P2`）—— 仅类型级 swap_group + 方案级 style_prompt，无同槽位跨风格素材变体。

### 5 golden 基线重冻（🔒 闸门型，需人工目检 PNG，攒齐一次开）
- 🔒 **golden 字节级重冻批**—— 主要欠：**层高参数化**（`WALL_H` 仍硬编码 1450）+ 组标签按几何中心对齐 + **厨卫 rooms 细分**。（门框/玻璃门/double/地板缝/layout 并集/prompt 方位并集已以字节安全方式落地。）
- ⚪ **P7 可选尾巴**（明确不排期）—— 幽灵 diff / 实时预览 / mm 双向输入 / 触控工具条 / 墙面材质 B spike。

### 6 部署与数据安全
- 🟢 **运维长尾清理批**—— 代码侧可查：dev 无 rsvg 硬 500 无降级 / 缩略图 kind 未入 `modes.py` 注册表 / real-render 角标显机器 `room_id`（如 `r_live`）应显中文房名。（服务器态：docker prune / 冗余 vhost / systemd 路径 —— 运维执行，代码查不出。）
- ⚪ **CDN 缓存绕过 Basic auth 加严**（合规）—— 图片端点无签名/令牌，PIPL 照片仅靠 uuid + 站点级 Basic auth，边缘缓存命中绕过 auth 风险成立。
- 🟢 **project_lock 破锁 TOCTOU**（`P2` 低）—— 陈旧锁自愈已有，破锁仍走 stat-mtime+unlink 非原子，改 `flock/fcntl`。
- ⚪ **规模化架构**—— 文件存储 + 单 worker(`JobManager` 进程内 dict) + Basic auth；PG/对象存储/Redis-RQ/多 worker/托管鉴权均未实现。

### 7 前端工作台外壳与鉴权
- 🟡 **鉴权/登录/会话/退出 + 数据访问控制**—— 用户区静态占位，无任何鉴权逻辑；解锁多项目/搜索/设置的前置，**需产品/架构决策**。
- ⚪ **顶栏全局搜索**—— demo 框已删，无检索后台。
- ⚪ **全局设置页真实业务**—— 仅「即将上线」空态。
- 🟢 **comingSoon 死代码 + admin demo/FullCalendar 清理**—— 纯清理：comingSoon 字段+UI 分支为死代码；admin demo 整套仍在；FullCalendar 依赖仍在。
- ⚪ **根级 NoSSR 收窄 SSR**（`P2`，审查报告唯一未处理 HIGH 性能项）—— `AppWrappers` 仍 `dynamic(ssr:false)` 包整站。

### 8 方案中心
- ⚪ **方案对比增强**—— compareIds 无持久化、无 sticky 迷你条、三栏放大用单图 lightbox 无同步缩放。
- ⚪ **方案搜索/筛选/排序 + 批量归档删除**—— 有徽标+单条归档，无搜索/筛选/排序/批量。
- ⚪ **空态/加载态/错误态统一打磨**（`P2`）—— 空缩略图裸灰字、错误 banner 无重试、骨架不贴合方案卡。

### 9 项目创建与多项目
- 🟡 **项目创建向导 + 下线 D-only**—— `MVP_D_ONLY=true`，后端只写单房间 starter；描摹/标定能力已具备但未串成向导。**是否现在开多项目是产品战略决策**。

### 10 产品边界与独立里程碑（🟡 需产品确认是否值得开）
- 🟡 **户型图/照片→矢量几何自动化**（时间盒试点）—— 全库无自动矢量化实现。
- 🟡 **任意角度/斜墙曲墙几何**—— geometry 全建于轴对齐 AABB，异形靠 merge 组近似；真支持需核心重写。
- 🟡 **45°/8 向轴测视角**—— 仅 4 正交视角（后端白名单 `PHOTO_DIRECTIONS` 拒非 v0..v3）；ROI 存疑。

### 11 测试与文档
- ⚪ **前端自动化测试矩阵**（`P1`）—— apps/web 仅 2 条 e2e，无组件/hook/集成测试，无 vitest/jest。应与画布重构同批建。
- 🟢 **编辑器升级计划文档 P3-P6 状态列更新**—— 文档状态列过时（P3/P5/P6 已实现却标待做，实际仅 P4 route-group 未做），回填即可，搭车其他 PR。

---

## D. 排期建议（按依赖）

**🟢 最先做（成熟、无产品依赖）**
1. **异地备份桶配置 + 真恢复演练**（文档定的 MVP 红线，脚本已全就绪，只差配桶跑一次）—— 最高价值/最低不确定性。
2. **运维长尾 + 前端死控件**清理批（rsvg 降级 / 缩略图入 modes / 角标中文名 / comingSoon 死代码 / admin demo / TOCTOU flock / 文档状态列）—— 小改无争议，合成一批。

**🔒 闸门（人来一次性做）**
3. **golden 重冻批**：层高参数化 + 组标签对齐 + 厨卫细分 —— 攒齐一次开人工目检 PR。

**杠杆项（需前置护栏）**
4. **Prompt Compiler 收尾**（lighting/must_keep/分层）—— 前置：先把 **评测集常态化运营** 立起来当对照。
5. **软装层结构化补全** —— Compiler 的自然延伸。

**🟡 需产品先拍板（决策解锁工程）**
- 是否开**多项目模式**（决定项目创建向导 / 户型图→矢量 / 任意角几何是否一起排）。
- 是否上**鉴权体系**（解锁用户区/搜索/设置/数据访问控制一整片）。
- 悬空 cut 洞语义（自动降级 vs 保留手动）。

**低优先重构（合批、需人工目检回归）**
- 双数据源深层收敛 + 命名统一，优先级低于功能与数据安全。

---

## 附：已确认闭环（不在 backlog）
- **AI 落位避门**：PR#6 / PR#39 / PR#32 已闭环。
- **画布交互能力**（undo/多选/视口/家具库/辅助线/HUD）：canvas-S01→S5b（2026-06-28）已实现。
